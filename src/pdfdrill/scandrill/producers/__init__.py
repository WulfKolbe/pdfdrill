"""Ingestion producers. Each turns a source of page images into Manifest entries.

A) find/folder  -> scandrill.ingest.add_folder
B) drag & drop  -> (drillui bridge upload endpoint, later)
C) camera       -> (bridge capture, later)
D) ADF scanner  -> scandrill.producers.adf
"""
