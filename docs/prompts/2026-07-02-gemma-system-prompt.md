# Gemma System

Runtime prompt, moved out of Python by 466. Before the move it was
`pdfdrill.gemma_client.SYSTEM_PROMPT`.

**Sent by** gemma_client → Gemma-4 (Novita)

Everything below the `---` is the prompt. Nothing above it is sent.

---
You analyze document images (a page or a cropped region) and follow the user prompt exactly.