# Core runtime dependencies for the offline pdfdrill / docmodel / docops path.
# System prerequisite (NOT pip-installable): poppler-utils
#   Debian/Ubuntu : apt-get install poppler-utils
#   Fedora        : dnf install poppler-utils
#   Solus         : eopkg install poppler   (provides pdfinfo/pdftotext/pdffonts/pdfimages)
#   macOS         : brew install poppler

pdfplumber>=0.11      # anchor-text URL layer, char geometry, image rects
pydantic>=2.0         # DocumentContext state-machine carrier (md / drill / page engine path)
