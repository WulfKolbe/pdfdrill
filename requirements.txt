# Core runtime dependencies for the offline pdfdrill / docmodel / docops path.
# System prerequisites (NOT pip-installable):
#   ghostscript   — the ONLY page rasterizer (>=400 DPI; OCR/vision/layout/
#                   image-locate all render via `gs`). Required for any raster task.
#     Debian/Ubuntu: apt-get install ghostscript   Fedora: dnf install ghostscript
#     Solus: eopkg install ghostscript             macOS: brew install ghostscript
#   poppler-utils — pdfinfo/pdftotext/pdffonts/pdfimages (NOT used for rasterizing)
#     Debian/Ubuntu: apt-get install poppler-utils  Fedora: dnf install poppler-utils
#     Solus: eopkg install poppler                  macOS: brew install poppler

pdfminer.six>=20221105  # born-digital text layer (model char dump), font-span leg
pdfplumber>=0.11      # tables, image rects, anchor-text URL layer
pydantic>=2.0         # DocumentContext state-machine carrier (md / drill / page engine path)
pypdf>=4.0            # form-field reading + document-level attachment fallback (pdfdrill formfields/attachments)

# Optional NLP enhancement (docops StanzaNlpMutator). Heavy + needs a one-time
# model download, so it lives in the [nlp] extra rather than the core path:
#   pip install 'pdfdrill[nlp]'
#   python -c "import stanza; stanza.download('en')"
# stanza>=1.10
