566 (final) — the rename, done; the range, not done, with a reason
==================================================================

1. THE RENAME — COMPLETE
-------------------------
One line, `docops/projectors/tiddlywiki.py:493`:

    title[s.id] = f"{bibkey}_H{i+1}"      ->  f"{bibkey}_H{i+1:04d}"

It was the ONLY unpadded identifier of ten — EQ, FO, PARA, PIC, FN, SN, LI,
LST are `:04d` and ABS, TOC are `:02d`. `_H` alone was bare.

  Section objects renamed                            314
  references retargeted (all in the tiddlers)     19,858
  references left in the old form                      0
  FILES left named in the old form                     0

The retarget was smaller than 462's because the name is generated at
PROJECTION time, not stored: `_H<n>` appeared in no model, no report.tex, no
report.ink.json and no report.tables.json. It appeared in the tiddlers'
`parent_section` (19,858), `title` and `text` — and re-projecting all 21
rewrote every one.

ONE PLACE IT DID SURVIVE, and only a full-tree search found it: cardona
carried an OKF export, `okf/cardona2013/sections/cardona2013_H1.md` … `_H24.md`,
24 files whose NAMES were the identifiers, plus 48 links in its `index.md`.
Re-projecting could not touch those. Regenerating the bundle fixed it — and
it now writes to `okf/cardona-qft-methods/`, so the export had a stale bibkey
too. A reference in a FILENAME is invisible to a content search, which is
the part of 462's lesson this nearly repeated.

2. THE RANGE — NOT APPLIED, AND THIS IS THE REPORT
----------------------------------------------------
flow_index is the right key: it is a single document-wide ordering (johnston,
12,939 objects, 12,938 distinct values, one collision) where (page, line) is
page-local. That part of the decision is confirmed.

But applying it to THESE Sections would be wrong, and the measurement says
so before the change rather than after:

  objects that would CHANGE section        58,594  of 69,248 with one — 85%
  objects that would GAIN one                 268

An 85% reassignment is not a correction. 570 explains it: johnston's last
Section sits at flow 5894 of 12938, so a flow-ordered range hands the whole
second half of the book to `Definition 2.D.1` — a theorem environment. The
range would be arithmetically correct and semantically nonsense.

The range is not the defect. The Sections are: 314 objects that are 162
headings doubled, mostly theorem environments, produced by a route that
finds 153 of 2,215 section headers and misses every chapter.

So: rename applied, range held. Fixing the range on top of route A would
make a wrong answer tidier.

Tests: 2,936 passing.
