2026-08-25T20:06:14Z  base=c31565005e95
186 — the floor of 7, restated against a POOLED measurement
==============================================================================
Superseded version below the line. This head is the pooled result the earlier
draft asked for and did not have: 9 documents, 632 clean conf >= 0.9 rows,
measured with inkdrill compare on their own report pages. The earlier draft
measured 0902.0431 alone, which out/066 shows is the corpus outlier.

THE FINDING SURVIVES THE CORRECTION. THE MAGNITUDES DO NOT.
------------------------------------------------------------------------------
   POOLED, 632 rows          distance  p10 3   p50 17   p90 53   max 407
                             comp delta        p50 8    p90 25   max 262

   class counts, current settings vs a raised distance floor:

      floor  7, Δcomp 2    K=11  N=101  W=18  S=0  C=502
      floor 53, Δcomp 2    K=11  N=118  W= 1  S=0  C=502     <- C UNCHANGED

Raising the distance floor from 7 to the measured p90 of 53 moves NOT ONE ROW
out of the component class. `flag_of` tests `comp_delta > 2` before it tests
the distance, so 502 of 632 rows are decided before the floor is consulted.

That was the earlier draft's conclusion from one document and it holds on nine.
What does not hold is the size of it: on 0902.0431 the component class took
96.2% of clean rows; pooled it takes 79.4%. Bad, but not the near-constant
function the outlier suggested.

   floor  7, Δcomp 9 (the measured p90)   K=11  N=130  W=429  S=0  C=62
   floor 53, Δcomp 9                      K=11  N=544  W= 15  S=0  C=62

   population change at Δcomp 9:  C 502 -> 62   (-440 of 632 rows, -70%)

ONE CLAIM FROM THE EARLIER DRAFT IS WITHDRAWN
------------------------------------------------------------------------------
I wrote that K (clean) is UNREACHABLE — 0 of 160 rows, and that an exact
five-way tie cannot occur. Pooled, 11 of 632 rows (1.7%) sit at distance
exactly 0. K is rare, not impossible, and I generalised an absence from the
one document least likely to produce it.

S (stable) does survive: 0 of 632 rows are scale-stable, on top of 0 of 160
before. Across every row I have measured, nothing has ever earned S.

WHERE THE FLOOR ACTUALLY CAME FROM — THE CATEGORY ERROR, LOCATED
------------------------------------------------------------------------------
out/066 measured 3,392 clean conf >= 0.9 rows across 49 documents and found
21.4% at ink >= 6. My pooled figure is 19.9% at or BELOW 6. Those are not the
same population read two ways; they are opposite.

The reason is that they are two different comparisons wearing one word:

   out/066 / out/052   MathPix's latex rendered STANDALONE at 400 dpi,
                       against the scan crop of the page region.
                       This is where 6 and 7 were derived.

   fill_ink_fields     inkdrill compare on a REPORT PAGE at 200 dpi, taking
                       the Rendered column cell against the Scan column cell
                       through the row lattice.
                       This is where 7 is APPLIED.

Different renderer, different resolution, different framing, different
segmentation. The floor was measured on one pipeline and is used to classify
another, and the two disagree about four fifths of their rows — which is
exactly the shape of every other finding this month, arrived at from the
opposite direction.

THE PER-DOCUMENT SPREAD, WHICH ARGUES AGAINST ANY GLOBAL FLOOR
------------------------------------------------------------------------------
   0707.4470       n= 51   p50  3.0   82.4% at or below 7
   1407.4737       n= 32   p50  7.0   53.1%
   1205.5935v1     n= 61   p50 14.0   27.9%
   0806.1585       n=117   p50 16.0   18.8%
   1411.5002       n= 57   p50 20.0   15.8%
   0210081         n= 73   p50 20.0    9.6%
   1205.3463v2     n=104   p50 17.5   10.6%
   1603.04246v2    n= 67   p50 22.0   11.9%
   1510.06699      n= 70   p50 30.0   11.4%
   (0902.0431      n=105   p50 27.0    8.6%   — the outlier, for comparison)

A tenfold spread in the fraction a fixed floor admits, 82.4% to 8.6%. One
constant cannot serve that, and the document that produced the constant is not
the document it is hardest on.

WHAT I RECOMMEND, UNCHANGED IN DIRECTION AND NOW BACKED BY 632 ROWS
------------------------------------------------------------------------------
   1. The distance floor is not worth arguing about. 7 or 53, it moves no row.
      Leave it at 7 and stop quoting it as the gate.
   2. NOISE_COMP_DELTA = 2 is the gate. Its measured p90 on clean rows is 9.
      Raising it to 9 moves 440 of 632 rows out of `component`.
   3. Nothing should be changed without the pages session, because six
      published documents carry these bullets and their deploy gate requires
      the index and the artefact to move together.
   4. Remove S from the legend or define it so a row can earn it.

I have still not changed the constants. They are the auditor's, they are
published, and this measurement — 9 documents, one corpus, one machine — is
evidence for a conversation, not a licence to re-class someone else's artefact.

==============================================================================
BELOW: the earlier draft, measured on 0902.0431 alone. Kept because its
reasoning is sound and its numbers are the outlier's, and because deleting a
superseded measurement hides how the corrected one was reached.
==============================================================================

EVERY GATE USING THE FLOOR, AND WHAT IT DOES
------------------------------------------------------------------------------
   fill_ink_fields.py:58   NOISE_DISTANCE   = 7
   fill_ink_fields.py:59   NOISE_COMP_DELTA = 2
   fill_ink_fields.py:70   flag_of() -> K / N / W / S / C
   scratchpad remeasure/measure.py:21   the same two constants, copied

   consumers: every report.ink.json; the Residual column of every published
   report; the C/W/S/N/K legend; 6 of the 10 documents on the site.

There is no floor of 7 anywhere in pdfdrill's own source. `refine` has none —
it accepts on a strict fall, `after < before`, which needs no threshold.

TWO CLASSES ARE UNREACHABLE
------------------------------------------------------------------------------
   K (clean)  requires distance == 0.  Measured: 0 of 105 clean rows,
                                       0 of 55 doubted rows. 0 of 160.
   S (stable) requires scale-stability. Measured: 0 of 105, 0 of 55.

The legend on every published report advertises five classes and the data can
produce three. K and S have never been earned by any row I have measured, and
K cannot be: two independent rasterisations of the same page differ somewhere,
so an exact five-way tie does not occur.

That is the same defect as the legend that promised a residual column seven
documents did not have — a key naming something the artefact cannot contain —
and it is still there, one level down.

WHICH THRESHOLD CHANGES, AND BY HOW MANY ROWS
------------------------------------------------------------------------------
Sweeping NOISE_COMP_DELTA with the distance floor held at the clean p90 (96),
and asking the only question that matters — does the class SEPARATE doubted
rows from confident ones:

   Δcomp   C on clean (conf>=0.9)   C on doubted (conf<=0.5)   separation
      2            96.2%                    100.0%               3.8 pp   <- now
      5            80.0%                     98.2%              18.2 pp
      9            64.8%                     96.4%              31.6 pp
     13            46.7%                     85.5%              38.8 pp
     20            29.5%                     69.1%              39.6 pp   <- best
     30            18.1%                     47.3%              29.2 pp
     39             9.5%                     36.4%              26.8 pp
     80             2.9%                     20.0%              17.1 pp

At the current setting the component class fires on 96.2% of rows MathPix is
certain about and 100% of rows it doubts. It does not discriminate: it is very
nearly a constant function that says "component" and it costs 3.8 points of
separation for the trouble.

POPULATION CHANGE at the widest separation (Δcomp 20, floor 96):

   conf >= 0.9   (n=105)   C 101 -> 31  (-70)   N 4 -> 73  (+69)   W 0 -> 1
   conf <= 0.5   (n=55)    C  55 -> 38  (-17)   N 0 -> 17  (+17)   W 0 -> 0

   Read that first line plainly: 70 rows MathPix is certain about would stop
   being called a component difference, and 69 of them would be called noise —
   which out/119 already established is what they are, 18 of 20 typographic
   with not one carrying a symbol MathPix got wrong.

   published: 0902.0431's re-measured 55 rows are currently 55 component /
   0 of anything else. They would become 38 component, 17 noise. The other
   five documents carrying a residual column are not re-measured here, so I
   will not state their movements — that needs the measurement, not a
   proportion applied to a count.

WHAT I AM NOT DOING, AND WHY
------------------------------------------------------------------------------
I am not changing the constants. Three reasons, in order of weight:

1. `fill_ink_fields.py` is not mine. It lives in ~/Downloads and belongs to the
   auditor's workflow; the constants are documented there as "inkdrill's
   measured floors (tools/findings.py) — do not invent new ones". Editing
   another party's calibration on my own evidence is exactly what that comment
   forbids, and it would be worse here because the numbers ARE published.

2. Changing them re-classes every residual bullet on the live site. Six
   documents, and the pages session runs a deploy gate that asserts the
   index's residual bars match the ink.json they describe. A re-class is an
   index change and an artefact change in the same commit, on their side.

3. My sweep rests on 160 rows from ONE document. The separation optimum at
   Δcomp 20 is a single number from a single book, and a threshold set on one
   document is how a floor of 7 came to be applied to a metric it was never
   measured against.

WHAT I RECOMMEND INSTEAD
   Measure the row metric on clean high-confidence rows across several
   documents — the harness is written and takes about four minutes a document
   — and set NOISE_COMP_DELTA from that pooled distribution. The distance
   floor can stay at 7 or move to 96; on this evidence it makes no difference
   to any row, and saying so is more useful than changing it.

   And whatever is chosen, K and S should be removed from the legend or given
   a definition a row can actually satisfy. A key that lists two classes
   nothing can earn is not a key, it is a claim.
