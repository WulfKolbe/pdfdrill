650 — the drill is idempotent: FN and REF counts settle
=========================================================

MEASURED FIRST (rule 14), on THIS tree, before touching code
  penev_A: `model --force` (cached MathPix branch only — no `mathpix --force`
  anywhere in this task), then three consecutive `bash tools/
  drill_full_sandbox.sh` runs. With the in-flight `adopt_target`/empty-refnum
  fix already applied (the working-tree state this task started from):

      kind       run1  run2  run3
      Footnote     48    50    51     <- MOVES, every run
      Reference    52    52    52
      cites        81    81    81
      claimed_0   243   243   243
      claimed_x    42    42    42
      Formula     674   674   674     <- identical every run
      Paragraph   222   222   222     <- identical every run
      Equation    116   116   116     <- identical every run

  So the in-flight fix (adopt_target accepting an empty refnum) is REAL but
  NOT SUFFICIENT: Footnote still grows every run. Confirmed by re-running the
  exact sequence twice; both times FN grew by 2 then 1 (48->50->51). Evidence:
  ~/pdfdrill-library/penev_A/out/650/{run1_full.json,run2_full.json,
  run3_full.json} (script.py, run at 2026-09-09 14:09-14:32, same code as
  committed here before this task's own fix).

THE MOVER, NAMED (rule 12 — diff the object sets, don't guess)
  Diffing Footnote titles run1 -> run2: `penev_A_FN0049`, `penev_A_FN0050`
  appear, nothing disappears (purely additive, matching the user's own
  report of stable titles elsewhere). Both new objects: `refnum=""`,
  `added_by`/`filled_by = footnote_cleanup`, `page=6`. Their content is NOT
  a genuinely new footnote — one of them is the SAME "We wish to thank Prof.
  M. J. Feigenbaum..." body already sitting in Footnote 9 (title
  `penev_A_FN0011`, the 11th title SLOT, not printed number 11), now wrapped
  in `{{penev_A_FN0011||FN}}` transclusion syntax instead of its original
  `\({ }^{9}\)` label.

  THE MECHANISM (found by tracing one object end to end, not by reading the
  existing 650 comment and trusting it — that comment names the SHAPE but
  the actual defect is one level deeper than it says):

  1. `cmd_model` (src/pdfdrill/commands.py ~4900) already calls
     `heading_cleanup.extract_footnote_paragraphs` once, unconditionally,
     right after `docmodel.main.run()` returns — NOT only via `pdfdrill
     clean`. On penev_A this lifts the labelled footnotes out of a
     Paragraph that `ParagraphProcessor` had merged with unrelated trailing
     prose (a `footnote`-typed line breaks paragraph grouping, but its
     CHILD lines are typed `text` — an ordinary PROSE type — so nothing
     stops the very next real paragraph's lines from riding along in the
     same group). `extract_footnote_paragraphs` correctly shortens
     `Paragraph.props["text"]` to just the surviving prose ("sensory
     stimulus \(\phi\)...", 92 of the original 1183 chars) — but does
     NOTHING to the Paragraph's `surface` Realization on `mathpix_lines`,
     which still spans all 14 original anchors (footnote text included).
  2. `pdfdrill clean` (the drill's own `clean` step) calls
     `extract_footnote_paragraphs` again (no-op now — the footnotetext is
     gone from `props["text"]`) and then `materialize_transclusions`.
     `materialize_transclusions` rebuilds a paragraph's text from the
     TiddlyWiki projector's OWN rendering (`_projected_paragraphs` ->
     `TiddlyWikiProjector._transclude_paragraph`) — and THAT function reads
     the Paragraph's `surface` Realization directly (`stream.slice_anchors
     (surface.start, surface.end)`), never `props["text"]`. Confirmed live
     (a controlled experiment against a fresh `model --force` build,
     src/docmodel/modules/paragraph.py + heading_cleanup, no drill): the
     module docstring's claim "the projector rebuilds transclusions from the
     immutable source STREAM, so this is idempotent" is true of the stream
     and false of the claim — the REALIZATION (an anchor SPAN into that
     stream, recorded on the object) is what actually gets read, and it is
     stale.
  3. So `materialize_transclusions` re-renders the FULL original 14-anchor
     span, including the already-extracted footnote content — now with its
     `\({ }^{N}\)` label replaced by `{{<fn>||FN}}` (the projector's own
     in-text footnote-marker substitution, since a real Footnote object now
     exists for that number) — and overwrites `props["text"]` with it,
     undoing step 1's extraction.
  4. The NEXT `extract_footnote_paragraphs` (next drill's `clean`) finds
     `\footnotetext` in this text again, but `fsplit.split_bodies` can no
     longer find a `\({ }^{N}\)` label (it is now a `{{||FN}}` token), so the
     WHOLE block is treated as one refnum-less body — `adopt_target`'s
     (page, refnum, extent) match can never fire for a labelled Footnote
     from a refnum-less probe (638's own rule, deliberately) — and a fresh,
     unrecognisable duplicate is minted. Repeat every drill, unboundedly.

  Reproduced at unit scale (tests/test_footnote_dedup.py,
  `test_without_the_flag_materialize_would_have_reintroduced_it`): a
  `footnote` block immediately followed by trailing prose in the same
  ParagraphProcessor group, built through the REAL FootnoteProcessor +
  ParagraphProcessor + TiddlyWikiProjector, produces exactly this cycle when
  the fix's flag is stripped — 2 Footnotes for one printed body after one
  extract + materialize + extract round trip.

THE FIX (src/pdfdrill/heading_cleanup.py)
  `extract_footnote_paragraphs`: when a Paragraph's text is SHORTENED
  (its footnotetext span removed but prose survives), it now also sets
  `props["footnote_extracted"] = True` — the same "this field was edited,
  don't re-derive it" signal `text_source`/`is_translated` already give a
  translated paragraph, kept as its OWN flag rather than reusing
  `text_source` so a footnote-shortened paragraph is never mistaken for a
  translated one by `classify.has_translation`/`docinspect.
  element_translations` (both key on that exact twin; `merge_latex.py`
  already reuses `text_source` for a third, unrelated purpose, so a FOURTH
  meaning on the same key was judged one collision too many).

  `materialize_transclusions`: skips a Paragraph carrying
  `footnote_extracted`, exactly like it already skips a translated one —
  never re-derives from the stale `surface` Realization. Tradeoff, stated
  rather than hidden: such a paragraph's OWN surviving inline math (e.g.
  `\(\phi\)`) no longer gets transcluded to `{{||FO}}` for llmtext/semantic/
  markdown — a small, accepted quality cost against unbounded duplicate
  Footnote creation.

  `footnote_extent.adopt_target` (already in flight when this task started,
  kept as-is): accepts an empty `refnum`, but only ever merges a refnum-less
  body with ANOTHER refnum-less one — an empty refnum still never matches a
  numbered Footnote by extent alone (638's rule). Genuinely needed for the
  "a group with NO label at all" shape (docs/... penev_A page 6 has one);
  orthogonal to the mover above, which is about a LABELLED footnote's own
  paragraph residue, not an unlabelled body.

  Tests added (tests/test_footnote_dedup.py, appended). Exactly which fail
  without the fix, checked directly (not assumed — see fix round 1 below
  for how this table itself was corrected): `test_extract_shortens_text_
  and_flags_the_paragraph`, `test_materialize_never_reintroduces_an_
  already_extracted_footnote_body` (end-to-end, the REAL TiddlyWiki
  projector, no mock), `test_a_regenerated_unlabelled_paragraph_is_
  adopted_not_duplicated`, and `test_adopt_target_merges_two_refnum_less_
  extents_that_overlap` — all four fail on 7bc6f63b (pre-fix) with only the
  new test file overlaid, and pass post-fix. `test_without_the_flag_
  materialize_would_have_reintroduced_it` is NOT among these: it strips
  `footnote_extracted` itself before asserting, so it passes identically
  pre-fix and post-fix by construction — it pins the FLAG, not the fix (see
  fix round 1).

FIX ROUND 1 — three findings from review (task-650-review.md), all real
  1. IMPORTANT, under-scoped by the review's own "minor" rating (the
     coordinator's own read, checked before deciding rather than deferred
     to): `TiddlyWikiProjector._transclude_paragraph` (docops/projectors/
     tiddlywiki.py:1518, called from `_emit_tiddlers` for every real
     Paragraph tiddler) reads the SAME stale wide `surface` Realization
     `materialize_transclusions` was gated against — and was NOT gated
     itself. `materialize_transclusions` only copies the projector's
     rendering INTO `props["text"]`; the projector's OWN output (the
     actual `.tiddlers.json` a reader opens) was never protected. MEASURED
     before deciding (rule 14): on penev_A, for all 3 `footnote_extracted`
     Paragraphs, the emitted tiddler text differed from `props["text"]`
     100% of the time (3 of 3) — each one still carried its extracted
     footnote body, marker replaced by `{{<fn>||FN}}`, permanently and
     deterministically (never breaking the count/title-set idempotency
     this task measures, which is exactly why it slipped through). One
     example, obj_f4efb16b3039 (`penev_A_PARA_0022`):
       props["text"]: "sensory stimulus \(\phi\), which we previously
                       denoted with \(\phi(\mathbf{x})\). Therefore,"
       emitted      : "\footnotetext{\n{{penev_A_FN0011||FN}} We wish to
                       thank Prof. M. J. Feigenbaum for the suggestion of
                       this clarifying language ... Sections are based on
                       {{penev_A_REF_Feigenbaum1997||CIT}} ..." (1000+
                       chars, the whole extracted block)
     This is the user-visible defect 636 was chartered against, in a
     consumer this task's own commit message wrongly described as gated
     ("materialize_transclusions...skips such a paragraph, exactly like it
     already skips a translated one" — true of the helper, false of "the
     projector"). Fixed: `_transclude_paragraph` now checks
     `footnote_extracted` first and falls back to
     `latex_sectioning_to_wikitext(para.props.get("text", ""))` — the same
     fallback the function already uses when a Paragraph has no `surface`
     Realization at all. Re-measured after the fix: 0 of 3 mismatch on
     penev_A. Test: `test_the_production_tiddler_is_also_gated_not_just_
     the_helper` (tests/test_footnote_dedup.py), confirmed failing without
     this specific fix by reverting just tiddlywiki.py and re-running it in
     isolation (fails: emitted text carries the stale footnotetext block;
     passes with the fix restored).
  2. MINOR. Added the missing adversarial negative test for the
     empty-refnum adoption path: two DIFFERENT, non-overlapping refnum-less
     footnotes on the same page must not merge.
     `test_adopt_target_never_merges_two_non_overlapping_refnum_less_
     footnotes` (tests/test_footnote_dedup.py) — pins that
     `covered_anchors(doc, obj) & want` (anchor overlap, not mere page+
     empty-refnum match) is what keeps them apart, next to the existing
     positive case and the numbered-vs-refnum-less negative case.
  3. MINOR — evidence discipline. The sentence above ("Tests added...each
     confirmed failing first") was wrong: `test_without_the_flag_
     materialize_would_have_reintroduced_it` passes on BOTH pre-fix and
     post-fix code, because it manually pops `footnote_extracted` itself
     before asserting — it never depends on whether the fix exists, only on
     whether the FLAG (once present) is respected. Corrected above to name
     exactly which four tests fail pre-fix and to say what the fifth test
     actually pins (the flag's own effect, not the fix). The test's
     docstring is corrected the same way.

  Both code fixes verified by reverting each in isolation and re-running
  its own test (failed for the stated reason), then restoring.

  FULL SUITE (fix round 1): PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src
  python3 -m pytest tests/ -q -p no:cacheprovider -> 3372 passed, 0 failed
  (3370 before this round's 2 new tests — finding 1's
  `test_the_production_tiddler_is_also_gated_not_just_the_helper` and
  finding 2's `test_adopt_target_never_merges_two_non_overlapping_
  refnum_less_footnotes`; finding 3 corrected an existing test's docstring
  and this file's own prose, adding nothing; no regressions).

FIX ROUND 2 — re-review found finding 1 only partially closed, plus one
NEW BLOCKING finding (finding 5)
  Round 1's `_transclude_paragraph` gate stopped the footnote-body leak by
  falling back to `latex_sectioning_to_wikitext(props["text"])` for any
  `footnote_extracted` paragraph — the SAME bare fallback already used when
  a paragraph has no `surface` Realization at all. That fallback skips the
  ENTIRE substitution pipeline (`_apply_line_substitutions`/`_substitute_
  footnotes`/`_substitute_inline_pictures`/`_substitute_residual_inline_
  math`/`_substitute_eq_refs`). MEASURED before deciding, confirmed live on
  penev_A: 2 of the 3 `footnote_extracted` paragraphs had their OWN
  legitimate `{{<formula>||FO}}` transclusions replaced by raw `\(...\)`
  math in the production tiddler — trading one leak for a different,
  undisclosed loss, worse because it moved the tradeoff from
  `props["text"]` (already disclosed as llmtext/semantic/markdown-only in
  round 1) into the actual TiddlyWiki output.

  ROOT FIX, per the coordinator/reviewer's agreed direction: stop routing
  around the stale Realization and narrow it instead. New
  `heading_cleanup._narrow_surface_to_remaining(doc, para, remaining)`:
  when `extract_footnote_paragraphs` shortens a paragraph's text, it first
  tries to find an EXACT suffix or prefix of the paragraph's ORIGINAL
  per-line texts (`_para_lines`) matching `remaining` (whitespace-
  normalized), and if found, rewrites the paragraph's own `surface`
  Realization to span just those surviving anchors. `_transclude_paragraph`
  and `materialize_transclusions` then read a Realization that is no
  longer lying, through their ORDINARY path — full substitution pipeline,
  transclusions intact — instead of any special-cased fallback.

  WHAT HAPPENS TO THE FLAG. Per the coordinator: remove it if narrowing
  makes it unnecessary, keep it only if it still buys something. It still
  buys something: `_narrow_surface_to_remaining` only matches an EXACT
  WHOLE-LINE suffix or prefix of the paragraph's original `_para_lines`,
  so it refuses whenever `remaining` is not composed of whole surviving
  lines — `footnote_extracted` is set as a SAFETY-NET fallback in that
  case, exactly as round 1 left it — the disclosed tradeoff (losing that
  ONE paragraph's own transclusions) is accepted ONLY for the shape(s)
  narrowing cannot represent, not for every footnote-shortened paragraph.
  Confirmed on penev_A's own corpus: of the ORIGINAL 3 `footnote_extracted`
  paragraphs, 1 is now narrowed (flag removed, full transclusions
  restored) and 1 falls back to the flag (its surviving text is literally
  `"}"` — nothing to transclude, so the tradeoff costs nothing in
  practice). The THIRD is discussed below — round 2 also fixed a second,
  incidental bug the coordinator's finding did not name but the
  measurement surfaced.

  CORRECTION (650 review round 3, finding 7): the ORIGINAL wording above
  (and the `_mathpix_block_footnotetext_mid_line` test fixture) described
  the one real corpus fallback case as "prose on both sides of the removed
  span, disjoint anchor ranges." That is WRONG for the actual penev_A
  paragraph. Traced directly from its real MathPix lines: the footnote's
  own LAST segment (no "next" label to bound it, so `_footnote_extents`
  lets it run to the end of the group — the SAME over-reach that lets a
  footnote's body swallow real paper prose, a separate, pre-existing
  data-quality gap out of 650's scope) ends with a STRAY closing brace
  MathPix emitted one too many of. `_balanced` matches the FIRST brace
  that returns depth to 0 (the footnote's genuine close), so the SECOND,
  extra `}` lands in `text[pos:]` as a lone SUB-LINE FRAGMENT on the SAME
  anchor as the footnote body's own tail line — not a whole surviving
  line at all, let alone prose on two sides. `_narrow_surface_to_remaining`
  only matches WHOLE lines, so a fragment of one line can never match
  either the suffix or the prefix search, and it correctly refuses — for
  THIS reason. The mechanism (refuse on anything but an exact whole-line
  match) was never wrong; only the narrative explaining WHY the one real
  case refuses was. `tests/test_footnote_dedup.py` now carries BOTH
  shapes: `_mathpix_block_with_sub_line_tail_fragment` (the actual
  corpus trigger, new this round) and `_mathpix_block_footnotetext_mid_
  line` (kept, relabelled as a second, independently-real shape the same
  generic logic must also get right — not a claim about what the corpus
  exercises).

  A SECOND, INCIDENTAL BUG FOUND WHILE BUILDING THE FIX: a malformed
  footnotetext span (an unbalanced brace deep in MathPix's own LaTeX —
  `_balanced` returning -1) can leave `remaining` IDENTICAL to the
  original `text`: nothing was actually extracted. Round 1 still set
  `footnote_extracted = True` in this case (the condition was just
  `if remaining:`, true whether or not anything changed) — meaning this
  THIRD penev_A paragraph (page 6, a `\cos^2(...)` footnote block, 24
  legitimate `{{...}}` tokens) was ALSO losing every one of its own
  transclusions under round 1, a THIRD live regression the reviewer's
  2-paragraph sample did not happen to cover. Round 2 gates flagging AND
  narrowing on `_norm_ws(remaining) != _norm_ws(text)` — nothing changed,
  nothing to guard against, so this paragraph is now left alone entirely
  and renders through the ordinary, untouched path. Confirmed: its
  production tiddler is now BYTE-IDENTICAL, all 24 tokens, to what
  `TiddlyWikiProjector` (base commit 7bc6f63b, before 650 existed) would
  have rendered for it — because nothing about it needed fixing in the
  first place.

  THE REQUIRED TRANSCLUSION CHECK — enumerated, for all 3 originally-
  affected penev_A paragraphs, the production tiddler BEFORE 650 (base
  7bc6f63b's `TiddlyWikiProjector`, run against a model whose Realization
  was never touched) versus AFTER round 2 (current code):

    A. "sensory stimulus ..." (page 6, lines 70-83) — NARROWED.
       Before 650, the tail of the (leaking) render was:
         "...vector spaces can be applied to {{penev_A_FO0087||FO}}.\n}
         \nsensory stimulus {{penev_A_FO0088||FO}}, which we previously
         denoted with {{penev_A_FO0028||FO}}. Therefore,"
       After round 2, the WHOLE render is:
         "\nsensory stimulus {{penev_A_FO0088||FO}}, which we previously
         denoted with {{penev_A_FO0028||FO}}. Therefore,"
       The surviving-prose tail is BYTE-IDENTICAL between the two —
       `{{FO0088||FO}}` and `{{FO0028||FO}}` both present, in order, exact
       text around them unchanged. The leaked footnote content
       (`{{FN0011||FN}}`, `{{FN0012||FN}}`, `{{FO0086||FO}}`,
       `{{FO0087||FO}}`) is gone.
    B. "cos^2(...)" block (page 6, lines 115-128) — malformed brace,
       UNCHANGED (the incidental bug above). Before-650 and after-round-2
       renders are byte-identical: all 24 `{{...}}` tokens present in both,
       same order, same content. Never part of the leak; not touched by
       either fix.
    C. `"}"`-only (page 6, lines 146-166) — FALLBACK (narrowing found no
       exact suffix/prefix match). Before-650 rendered the full leaked
       block (12 tokens, including `{{FN0015||FN}}`). After round 2:
         `props["text"]` and the emitted tiddler are both exactly `"}"`  —
       0 tokens, because there is nothing left to transclude. The
       disclosed tradeoff (this ONE paragraph's own transclusions, if it
       had any) costs nothing here in practice.
    Every surviving-prose transclusion the 3 paragraphs carried survives.
    Script: penev_A/out/650/round2_transclusion_check.py (below).

  WATCHING THE LEDGER AND `docops.conserve`, BEFORE AND AFTER (as
  instructed — an unexplained delta is a finding; these are explained):

    penev_A (`model --force` + `clean` only, isolating the mechanism from
    the rest of the drill — the SAME two builds used for the transclusion
    check above):
                        round1 (before)   round2 (after)   delta
      ledger claimed_0        243               243          0
      ledger claimed_1       2893              2906        +13
      ledger claimed_multi     42                29         -13
      ledger block_claims    2977              2964         -13
      ledger inline_claims   1295              1295          0
      conserve unreachable    181               171         -10
      conserve unclaimed      243               243          0
      conserve doubly_claimed  42                29         -13
      INTENDED: narrowing the paragraph's Realization means the anchors it
      used to claim TOGETHER WITH the Footnote's own realization (a page-6
      block spanning ~13 lines) are now claimed by the Footnote ALONE —
      claimed_multi/doubly_claimed drop by exactly 13, claimed_1 rises by
      exactly 13, and claimed_0 (truly unclaimed) does not move: no anchor
      became orphaned, 13 anchors moved from doubly- to singly-claimed.
      This is the improvement the coordinator predicted. `unreachable`
      dropping by 10 is a plausible knock-on (fewer objects whose OWN
      claimed content routes to no tiddler) but was not traced further —
      named as a side effect, not re-derived line by line.

    SteerableWaveletFramesBasedontheRieszTransform (same protocol): ZERO
    movement on every count, round1 vs round2, because this document has
    ZERO `footnote_extracted` paragraphs (confirmed by direct query) — it
    has exactly 1 Footnote total and no page where a footnote's raw
    content ever merges into a following Paragraph the way penev_A's
    page 6 does. Nothing for round 2's mechanism to act on; the flat
    before/after IS the explained result, not an oversight.

  RE-VERIFIED THE WHOLE 650 CLAIM UNDER ROUND 2 (mechanism changed, so the
  claim is re-run, not assumed): three consecutive full drills, fresh
  `model --force` first, both documents:

    penev_A: Footnote 48/48/48, Reference 52/52/52, cites 81/81/81,
      ledger claimed_0 243/243/243, claimed_multi 29/29/29 (the round-2
      value, stable — down from round 1's 42/42/42, matching the isolated
      measurement above). Title sets identical run-to-run, every kind.
    Steerable: Footnote 1/1/1, Reference 30/30/30, cites 64/64/64, ledger
      claimed_0 132/132/132, claimed_multi 136/136/136 (this full-drill
      pipeline's own ledger baseline; NOT the same number as the
      model+clean-only isolation above — the two measure different
      pipelines, both internally stable). Title sets identical
      run-to-run, every kind. `ListItem` landed at 50/50/50 this round (a
      fresh sidecar, per the already-open `cmd_lists` cold-start finding
      below — not a NEW instability, and stable 3-for-3 as required).

  FULL SUITE (fix round 2): PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src
  python3 -m pytest tests/ -q -p no:cacheprovider -> 3377 passed, 0 failed
  (3372 before this round's 5 new tests: `test_narrow_surface_to_
  remaining_finds_the_suffix_and_the_prefix`, `test_narrow_surface_to_
  remaining_refuses_a_no_match`, `test_narrowing_fails_on_the_mid_line_
  shape_and_the_flag_engages`, `test_the_fallback_flag_still_gates_the_
  production_tiddler_when_narrowing_fails`, `test_narrowing_preserves_a_
  surviving_formulas_transclusion` — the last one is finding 5's own
  regression pin, confirmed failing against round 1 by reverting
  `heading_cleanup.py` to d65233a7 and re-running it in isolation. Three
  existing tests were also rewritten to match the new, correct behaviour
  (a suffix-shaped paragraph is narrowed, not flagged) rather than
  deleted: `test_extract_shortens_text_and_narrows_the_realization`
  (renamed from `..._and_flags_the_paragraph`), `test_without_the_flag_
  materialize_would_have_reintroduced_it` (now uses a mid-line fixture,
  since a suffix-shaped one is no longer flagged at all and popping a
  never-set flag would prove nothing), and `test_the_production_tiddler_
  is_also_gated_not_just_the_helper` (asserts the outcome — correct,
  footnote-free output — rather than the flag, since narrowing now
  provides it). No regressions.

FIX ROUND 3 — re-review confirmed finding 5 fixed and verified
independently; three new minor findings, all addressed (short round, as
the coordinator asked)
  6. `_narrow_surface_to_remaining` returning `False` (the fallback
     engaging) incremented no `doc.meta` counter, unlike its siblings
     `footnote_orphan_tail`/`footnote_span_not_located`/`footnote_adopted`
     in the SAME function, and `docops.conserve`'s `footnote_refusals`
     didn't surface it — so a future rise in fallback frequency (re-losing
     surviving transclusions at smaller scale, the way round 1 did at full
     scale) would move no existing metric; only per-object inspection
     would ever notice. Fixed: a new `not_narrowed` counter, incremented
     exactly where the flag is set, accumulated into
     `doc.meta["footnote_not_narrowed"]` the same way its siblings are,
     surfaced in `conserve.py`'s `footnote_refusals` dict AND in
     `commands.py`'s `_format_footnote_refusals` (the `status` formatter —
     the SAME function whose own docstring already says "a count nothing
     prints is a count nobody reads," 645). MEASURED on both documents
     (fresh `model --force`, cached MathPix): penev_A = 1 (the one real
     fallback case, below), Steerable = 0 (0 `footnote_extracted`
     paragraphs exist on that document at all, matching round 2's own
     finding). Test: `test_footnote_not_narrowed_reaches_status_and_
     conserve` (tests/test_footnote_dedup.py).
  7. The round-2 narrative and its `_mathpix_block_footnotetext_mid_line`
     fixture misidentified WHY the one real penev_A fallback case refuses
     narrowing — corrected above (in the FIX ROUND 2 section, marked
     CORRECTION) to the actual shape the reviewer traced: a SUB-LINE
     fragment (a stray extra `}` MathPix emitted, landing on the same
     anchor as the footnote body's own tail line — not a whole surviving
     line), not "prose on both sides." The mechanism was never wrong
     (refuse on anything but an exact whole-line match covers both
     shapes); only the explanation was. Added
     `_mathpix_block_with_sub_line_tail_fragment` — a minimal fixture
     reproducing the REAL trigger (a footnotetext group whose closing
     brace is followed, on the SAME anchor, by one stray extra `}`) — and
     `test_the_real_corpus_fallback_shape_is_a_sub_line_fragment`, which
     pins `n == 1` (the footnote body is still correctly extracted),
     `props["text"] == "}"`, `footnote_extracted is True`, the Realization
     left wide (untouched), and `doc.meta["footnote_not_narrowed"] == 1`.
     The mid-line fixture is kept (relabelled, not deleted) as a second,
     independently-real shape the same generic logic must also get right.
  8. The malformed-brace "nothing changed" gate
     (`_norm_ws(remaining) != _norm_ws(text)`) was correct but unpinned by
     any test — a future revert to round 1's bare `if remaining:` would
     not have been caught by the suite. Added
     `_mathpix_block_malformed_brace` (an unbalanced `\footnotetext{` with
     no closing brace at all — `_balanced` returns -1) and
     `test_a_malformed_unclosed_footnotetext_changes_nothing`, which pins
     `n == 0`, zero Footnotes minted, `props["text"]` byte-identical to
     the input, no flag set, no counter incremented, and the Realization
     completely untouched.

  All three verified as genuine regression pins, not decorative: reverted
  `heading_cleanup.py`/`conserve.py` to fix round 2's commit (fc98841b) in
  place, confirmed `test_the_real_corpus_fallback_shape_is_a_sub_line_
  fragment` and `test_footnote_not_narrowed_reaches_status_and_conserve`
  both fail with `KeyError: 'footnote_not_narrowed'` (the counter/dict key
  do not exist yet), confirmed `test_a_malformed_unclosed_footnotetext_
  changes_nothing` already passed under round 2 (the gate itself was
  correct, only untested — matching finding 8's own description), then
  restored round 3's code.

  RE-VERIFIED counts are unchanged by this round (it is purely additive —
  a counter, corrected prose, three tests — no behavioural change to
  extraction, narrowing, or rendering): three consecutive full drills,
  fresh `model --force` first, penev_A only (Steerable's own mechanism is
  untouched by this round — 0 `footnote_extracted` paragraphs — so a
  targeted single-run check sufficed there): Footnote 48/48/48, Reference
  52/52/52, cites 81/81/81, ledger claimed_0 243/243/243, claimed_multi
  29/29/29 — identical to fix round 2's own table, as expected. Title sets
  identical run-to-run. Steerable (one drill, matching round 2 exactly):
  Footnote 1, Reference 30, cites 64, ledger claimed_0 132, claimed_multi
  136.

  FULL SUITE (fix round 3): PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src
  python3 -m pytest tests/ -q -p no:cacheprovider -> 3380 passed, 0 failed
  (3377 before this round's 3 new tests: `test_footnote_not_narrowed_
  reaches_status_and_conserve`, `test_the_real_corpus_fallback_shape_is_a_
  sub_line_fragment`, `test_a_malformed_unclosed_footnotetext_changes_
  nothing`. No regressions.)

THE REFERENCE SIDE (task's item 2) — ALREADY FIXED, VERIFIED NOT REOPENED
  Read first, per the brief: tasks/010.report.md (stub creation moved to one
  `ensure_reference_stub` helper called by every Citation creator; round 3
  made the `--force` cleanup gate decide by `added_by` provenance) and
  out/648.txt (the BARE `bibliography` call: `_existing_citation_spans`
  makes `detect_numeric_citations`/`detect_author_year_citations` skip a
  span they already cover, and `add_reference_objects` skips a heuristic
  entry whose citekey already names a FILLED Reference). Both are in the
  same family the brief pointed at, and out/648.txt's own corpus check
  already measured penev_A (81 citations/52 references, all stubs) and
  Steerable (64 citations/29 heuristic-filled references) stable under
  `bibliography` run TWICE in a row, byte-identical message and Citation
  ids both times, on both documents.

  This task's job was to verify that holds under the FULL drill (`bibsource`
  -> `bibliography` -> `clean` -> ... in one pipeline, not `bibliography` in
  isolation) — measured, not assumed. Result: Reference is 52/52/52 on
  penev_A and 30/30/30 on Steerable across three consecutive full drills,
  same citekey set every run (diffed by title, not just counted), `cites`
  edges 81/81/81 and 64/64/64. NO mover found on either document. The
  Reference side needed no new fix; 648 + 010 already cover the full-drill
  path, and this task's own measurement is the confirmation that was still
  missing.

THREE CONSECUTIVE FULL DRILLS, AFTER FIX ROUND 1 — SUPERSEDED BY THE
ROUND-2 RE-VERIFICATION ABOVE (kept for the historical record; the
mechanism changed between the two, so `ledger claimed_multi` legitimately
differs: 42 here vs 29 after round 2's narrowing — explained in FIX ROUND
2's ledger/conserve section, not a discrepancy)
  penev_A (`model --force` once, then three `drill_full_sandbox.sh` runs,
  each verified "All 22 steps succeeded" before the next was started):

      kind        run1   run2   run3
      Abstract       1      1      1
      Diagram       14     14     14
      Equation     116    116    116
      Footnote      48     48     48
      Formula      674    674    674
      ListItem       4      4      4
      Page          22     22     22
      Paragraph    222    222    222
      Picture       11     11     11
      Reference     52     52     52
      Section       37     37     37
      Sidenote      46     46     46
      Table          1      1      1
      cites edges   81     81     81
      ledger claimed_0      243    243    243
      ledger claimed_multi   42     42     42

  Title SETS (by kind) identical run1==run2==run3 — diffed, not just
  counted; zero gained, zero lost, every kind.

  SteerableWaveletFramesBasedontheRieszTransform (same protocol; only ONE
  PDF sits beside this document, so the folder-name rule was not even at
  risk here, unlike penev_A's six):

      kind        run1   run2   run3
      Abstract       1      1      1
      Diagram       16     16     16
      Equation      70     70     70
      Footnote       1      1      1
      Formula      415    415    415
      ListItem      48     48     48
      Page          15     15     15
      Paragraph    167    167    167
      Picture        1      1      1
      Reference     30     30     30
      Section       17     17     17
      Sidenote      30     30     30
      Table          3      3      3
      cites edges   64     64     64
      ledger claimed_0      132    132    132
      ledger claimed_multi  134    134    134

  Title sets identical run1==run2==run3 on this document too.

FINDING, NAMED NOT ROUNDED OFF: a cold-start `cmd_lists` discrepancy
  A single ISOLATED first invocation of `pdfdrill lists` on a JUST-BUILT
  (`model --force`, brand-new sidecar with no prior evidence) Steerable
  model reported "17 nested Lists from 50 list items (2 merged bullets
  recovered via y-position re-split)"; every subsequent call on the SAME
  document (sidecar now carrying this session's accumulated evidence from
  today's other runs) reported "7 nested Lists from 48 list items" —
  consistently, repeatably, including two direct back-to-back `pdfdrill
  lists` calls with no drill in between. This is NOT the class 650 was
  chartered to fix (it is not Footnote/Reference, and it did not move
  across the three CONSECUTIVE drills the task measures — A1/A2/A3 above
  are byte-for-byte stable at 48 ListItems each). It is named here because
  a count moved between "first ever run" and "every run after," and rule 12
  says an unexplained delta is a finding, not something to round off by
  silently re-measuring until the number looks stable. Root cause not
  chased (outside this task's scope and budget): `cmd_lists`'s "y-position
  re-split" recovery heuristic reads something from accumulated sidecar
  state that a virgin sidecar does not yet have. Left OPEN. Fix round 2's
  own fresh `model --force` produced YET A THIRD stable value (50/50/50,
  a different sidecar history again) across its three consecutive drills —
  consistent with this same open finding (a fresh sidecar's `cmd_lists`
  baseline is history-dependent but stable once set), not a new or worse
  instability, and not re-chased for the same reason.

  (A separate, purely procedural incident during measurement, NOT a product
  defect: two Steerable drills launched back-to-back in the BACKGROUND with
  near-zero gap hit a transient `DocumentBusy` lock refusal — `doclock.hold`
  behaved correctly, refusing to interleave — most likely a PID-reuse
  collision in the staleness check rather than a real overlap, given the
  wrapper process the run was waited on had already exited. Fixed the
  measurement, not the code: every run reported in the table above was run
  to completion in the FOREGROUND, one at a time, each checked for "All 22
  steps succeeded" before the next was started. Also caught and corrected
  mid-task: restoring a document's `model.docmodel.json` by hand without
  also removing the stale `model.docpack.json` sidecar serves the packed
  cache instead of the file just restored — the exact 648 incident,
  repeated and caught the same way, by a sha/count mismatch against the
  expected pristine baseline.)

WHAT IS OPEN
  - The `cmd_lists` cold-start finding above (not Footnote/Reference; not
    reproduced across consecutive drills; root cause not chased).
  - `heading_cleanup.clean_heading_residuals` shortens a Paragraph's text
    (strips a leading LaTeX sectioning command) the same way
    `extract_footnote_paragraphs` used to — without touching the `surface`
    Realization AND without setting `footnote_extracted` or any guard of
    its own. Given fix round 1's finding 1, this means BOTH readers of the
    stale span (`materialize_transclusions` and, now confirmed the more
    important one, `TiddlyWikiProjector._transclude_paragraph` — the real
    output) are exposed for THIS shortener, not just the helper. It was not
    observed to duplicate or misrender anything on penev_A/Steerable (no
    new Section/Paragraph objects appeared across three drills on either
    document, and neither corpus document's `clean_heading_residuals` path
    was checked tiddler-by-tiddler the way the footnote path now is).
    Named, not fixed — outside this task's Footnote/Reference charter and
    the corpus in hand does not exercise the count-visible half of it. Fix
    round 1's measurement method (compare emitted tiddler text against
    `props["text"]`) was shown in round 2 to be BLIND to a lost
    transclusion (both sides are raw, untranscluded text — finding 5's own
    lesson) — the correct check, if this is ever picked up, is round 2's
    own method: compare the emitted tiddler against a PRE-650 render of
    the same (untouched) Realization, the way
    `round2_transclusion_check.py` does for the footnote path. Round 2's
    `_narrow_surface_to_remaining` helper is written generically enough
    (paragraph + remaining text -> narrowed or untouched) that
    `clean_heading_residuals` could call it directly rather than inventing
    a second mechanism, if this is picked up later.
  - The cross-detector citekey case mismatch out/648.txt already named
    (`detect_author_year_in_objects` folds to lowercase,
    `detect_author_year_citations` preserves case) is still open and still
    not reachable on penev_A/Steerable (neither carries `.bbl`/`.bib` gold).
  - Review finding 4 (INSPECT.txt / `expect=`) needed no action: the
    reviewer confirmed both documents' INSPECT.txt files already match
    HANDOVER-RULES rule 20 (bare paths, nothing else) and that no
    `expect=` convention exists anywhere in the repo — this task's own
    brief's premise for that file was wrong, not the files.

FULL SUITE: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest
tests/ -q -p no:cacheprovider -> 3380 passed, 0 failed (fix round 3).

EVIDENCE
  ~/pdfdrill-library/penev_A/out/650/{script.py,runs.json,messages.txt,
  INSPECT.txt,run1_full.json,run2_full.json,run3_full.json (pre-fix, the
  bug caught live),fresh/fix_r1.json,fresh/fix_r2.json,fresh/fix_r3.json
  (post fix round 1),round2_transclusion_check.py + its captured
  round2_transclusion_check_output.txt (the required before/after
  transclusion enumeration),fresh/before650_results.json (the base-commit
  7bc6f63b renders it compares against),fresh/model.round1_before_narrow.
  docmodel.json + fresh/model.round2.pristine.docmodel.json (the two model
  snapshots the check reads),fresh/round2_run{1,2,3}.json (fix round 2's
  own three-drill re-verification); `footnote_not_narrowed` = 1, measured
  directly (`doc.meta`/`conserve`/`status`, all three agree) on a fresh
  `model --force` build, fix round 3}
  ~/pdfdrill-library/SteerableWaveletFramesBasedontheRieszTransform/out/650/
  {script.py,runs.json,messages.txt,INSPECT.txt,fresh/A1.json,fresh/A2.json,
  fresh/A3.json (fix round 1),fresh/round2_run{1,2,3}.json (fix round 2);
  `footnote_not_narrowed` = 0, fix round 3}
  src/pdfdrill/heading_cleanup.py, src/docops/conserve.py,
  src/pdfdrill/commands.py (`_format_footnote_refusals`, fix round 3),
  src/docmodel/footnote_extent.py, src/docops/projectors/tiddlywiki.py
  (fix round 1, unchanged by rounds 2-3), tests/test_footnote_dedup.py —
  this repo, this commit + fix rounds 1-3. task-650-review.md (the review
  all three rounds answer) — coordinator's scratchpad, not this repo.
