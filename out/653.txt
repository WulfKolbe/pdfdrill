653 — one status page across the corpus
=========================================
(fix round 1 folded in: three BLOCKING findings from the review at
task-653-review.md, all addressed — the narrative rewritten from the real
artefact [finding 1], a third "unaudited" row state added and rendered
distinctly [finding 2], per-document exception isolation added to the
corpus walk [finding 3] — plus two minor findings [10, 11]. Corpus re-run
after every fix; every number below is read off that re-run's artefact, not
carried over from the first draft.

fix round 2 folded in: one new BLOCKING finding — round 1's exception
isolation covered the GATHER phase only; the RENDER phase
[`_render_corpus_index_html`'s row loop] had none, so one row raising while
being rendered would still void the whole index for every other row already
gathered — the identical catastrophic shape finding 3 was raised to
prevent, one phase later in the same pipeline. Fixed with the render-phase
twin of round 1's gather-phase fix [`_corpus_render_row_or_error`], plus a
minor: the page's summary paragraph now gives "errored" its own number
instead of folding it into "unavailable". Corpus re-run again after this
fix; the gather-phase numbers are UNCHANGED — confirmed by assertion in
`out/653c/script.py`, not merely restated.

fix round 3 folded in: a THIRD instance of the SAME defect — round 2's
render-phase wrapper protects `_corpus_row_html`'s call into
`_corpus_row_state`, but `_render_corpus_index_html` ALSO calls
`_corpus_state_counts`, which walks `rows` a SECOND, independent time and
calls the same fallible `_corpus_row_state` unguarded; a row with an
incomplete `conserve.counts` dict made it raise `KeyError` there too,
voiding the whole write AFTER the (correctly protected) render loop had
already survived the identical row. Three rounds finding the same failure
one call site later is the tell that the fix belongs in the function, not
in its callers: `_corpus_row_state` is now TOTAL — it never raises, for
any input, full stop — so both this call site and `cmd_corpusstatus`'s own
separate `_corpus_state_counts` call (which the review named as
"practically moot only because the crash upstream fires first," i.e. a
FOURTH latent instance) are safe by construction, and the two existing
wrappers become belt-and-braces rather than the load-bearing thing. Tested
directly on the malformed shapes, not only through a caller. Corpus
re-run again; the counts are BYTE-IDENTICAL to rounds 1/2 — asserted in
`out/653d/script.py`, which also calls `_corpus_row_state` on the same
malformed shapes directly and confirms none of them raises.)

WHAT CHANGED
  `pdfdrill corpusstatus [--library DIR] [--only-published]`: walks every
  document under the library and writes `index.html` at the library
  root — one row per document (model built + date, gold bibliography +
  count, the 656 conservation verdict, unclaimed anchors, how many of the
  five 632/633 evidence/residuals files are present), each linking to that
  document's own `status --html` page (652). Sortable by column in plain
  inline JS (no network). Read-only: never runs `pdfdrill model`, never
  takes a per-document lock.

  `src/pdfdrill/commands.py`:
    corpus_status_rows(library, only_published=False)   the corpus walker.
                        Default enumerates through `sidecar.iter_documents`.
                        `only_published=True` reads
                        `<library>/out/documents.json` instead — the small
                        named set `publishcheck.py` calls "the published
                        set". Returns `skipped` (fix round 1, finding 10):
                        how many published-set entries had no path or a
                        path that no longer resolves to a file.
    _corpus_row_or_error / _corpus_error_row   fix round 1, finding 3 — see
                        THE EXCEPTION ISOLATION FIX below.
    _corpus_status_row(pdf, blob_dir, library)   one read-only row. Never
                        calls `cmd_model`; reuses 652's OWN read-only
                        gatherer, `_status_conserve_ledger`, UNCHANGED, for
                        the conservation/ledger numbers.
    _corpus_row_state(r)   fix round 1, finding 2 — the ONE place a row's
                        state (`ok`/`unaudited`/`bad`/`unavailable`/
                        `errored`) is decided; read by both the row
                        renderer and the page's/command's own summary
                        counts so the two cannot silently disagree. See
                        THE THREE-STATE FIX below. FIX ROUND 3: now TOTAL
                        — wrapped in its OWN try/except, never raises for
                        any input — see THE TOTAL-FUNCTION FIX below.
    _corpus_state_counts(rows)   the one aggregator both
                        `_render_corpus_index_html` and `cmd_corpusstatus`
                        call — a second, hand-rolled count in each place is
                        exactly how finding 1 happened the first time. Safe
                        by construction since fix round 3 (calls the now-
                        total `_corpus_row_state`), not by its own guard.
    _corpus_render_row_or_error / _corpus_row_render_error_html   fix round
                        2 — the render-phase twin of round 1's gather-phase
                        fix; still present as belt-and-braces after round
                        3, no longer the ONLY thing standing between a
                        malformed row and a voided page.
    _corpus_model_date / _corpus_gold_bib / _corpus_artefacts /
    _href_from_library / _render_corpus_index_html / _corpus_row_html /
    _write_corpus_index_html   unchanged in shape from the first round
    (confirmed correct by the review, findings 4-9) except where noted
    above; `_write_corpus_index_html` still writes bytes with
    `errors="replace"` (the surrogate fix, review finding 7, unchanged and
    re-confirmed).
    cmd_corpusstatus(library=None, only_published=False)   the command;
                        its printed prose now reports all four states plus
                        how many rows the exception handler caught, and
                        (only_published) how many published-set entries
                        were skipped.

  `src/pdfdrill/cli.py` / `preflight.py` / `commands.yaml`: unchanged from
  round 1 (review findings 9: manifest/skillsync/preflight all confirmed
  correct; no reason to touch them this round).

THE THREE-STATE FIX (review finding 2)
  Round 1 rendered every non-"conserved" available row the same red
  `class="bad"`, whether the row carried a real anchor defect or was
  simply never audited against the 656 ratchet. `_status_conserve_ledger`
  already returns `has_baseline_row`; round 1's `_corpus_row_html` computed
  it and never read it. Fixed by giving the row ITS OWN state:

    ok         verdict == "conserved".
    unaudited  verdict != "conserved", AND unclaimed == 0 AND
               doubly_claimed == 0, AND NOT has_baseline_row. The ONLY
               thing wrong is an unbaselined violation type — no measured
               anchor defect at all. Rendered in its own colour
               (`--warn`, amber/orange, not red) with its own cell text
               ("unaudited — N unbaselined violation object(s), no
               baseline row recorded yet (0 unclaimed, 0 doubly-claimed)").
    bad        verdict != "conserved" and it is NOT the unaudited shape —
               i.e. a real unclaimed/doubly-claimed anchor (regardless of
               baseline status), OR a genuine regression against a
               baseline someone DID record (`new_types`/`increased` on an
               audited bibkey). Stays red.
    unavailable / errored   `conserve.available == False` — no model, a
               stale one, one skipped by the size guard, or (new) one this
               walk could not read at all. Both render the same dim/grey
               class (`unknown`) on the page — the reader-facing fact is
               the same ("no conservation number for this row") — but are
               counted SEPARATELY in the command's own prose (see below),
               because "the walk itself failed on this document" is a
               different, more actionable fact than "nobody has built a
               model yet."

  `_corpus_row_state(r)` is the ONE function that decides this, called by
  both the row HTML and the aggregate counts (`_corpus_state_counts`) — a
  second hand-rolled tally anywhere else is exactly the mechanism that
  produced finding 1's wrong narrative the first time, and this closes
  that seam rather than papering over its one visible symptom.

THE EXCEPTION ISOLATION FIX (review finding 3)
  Round 1's `corpus_status_rows` called `_corpus_status_row` directly in
  both loops with no try/except. `_status_conserve_ledger` pre-checks
  existence and staleness but does not itself guard `load_model`/
  `conserve`/`gate`/`ledger.materialize` against a corrupt or
  schema-mismatched model — a genuinely malformed `model.docmodel.json`
  (truncated write, bad JSON, an old schema `conserve()` cannot parse)
  would raise straight out of `cmd_corpusstatus`, aborting the ENTIRE walk
  and writing NO `index.html` at all — losing every row already computed
  for the other ~2,930 documents. `test_cwd_restored_even_when_the_
  gatherer_raises` (round 1) demonstrated the raise reaching all the way
  out; it is now rewritten to demonstrate the OPPOSITE (see TESTS).

  Fixed with one seam, `_corpus_row_or_error`, called from both loops in
  `corpus_status_rows`: `_corpus_status_row` wrapped in try/except, and on
  ANY exception a `_corpus_error_row` is appended instead — `errored:
  True`, `conserve.available: False`, `reason` naming the exception type
  and message, artefacts/folder computed defensively (each itself wrapped,
  so a broken `blob_dir` cannot cascade into a second, unhandled
  exception). Verified two ways, per the review's own request to show the
  handler working AND report what it did on the real corpus, separately:

    - CONSTRUCTED: `test_one_malformed_model_does_not_abort_the_whole_walk`
      writes a REAL invalid-JSON `model.docmodel.json` (not a monkeypatched
      stub) for one of three documents and confirms `cmd_corpusstatus`
      does NOT raise, `index.html` still gets all 3 rows, and the broken
      one reads `errored=True`, state `"errored"`, with the caught
      exception's type+message in its `reason`.
    - REAL CORPUS: re-ran the full walk on `~/pdfdrill-library` (2,931
      documents) AFTER this fix. **0 rows were caught by the exception
      handler on this run** (`errored_rows_on_this_real_run: 0` in
      `out/653b/report.json`, cross-checked against the actual
      `index.html` text — no row anywhere reads "errored — "). This is a
      fact about the CURRENT state of this library, not evidence the
      handler is inert: the constructed test above proves the handler
      fires and survives when a document IS unreadable; this run says none
      of the 2,931 documents encountered on THIS pass happened to be in
      that state (the 2 stale + 1 oversized are all caught earlier, by the
      existing staleness/size checks, before `_status_conserve_ledger`
      would ever raise on them).

THE RENDER-PHASE ISOLATION FIX (fix round 2 — new BLOCKING finding)
  Round 1 fixed the GATHER phase only. `_render_corpus_index_html`'s row
  loop — `body = "".join(_corpus_row_html(r) for r in rows)` — had no
  try/except at all, and neither did `_write_corpus_index_html`. A row that
  raised while being RENDERED (not gathered) would propagate straight out
  of `cmd_corpusstatus`, voiding the HTML write for every one of the other
  2,930 rows that had already been gathered successfully — the identical
  catastrophic failure finding 3 was raised to prevent, relocated one phase
  later in the same pipeline. No test exercised the render phase at all
  before this round.

  The defence that "it has not happened" is not a defence, for the same
  reason it was rejected for the gather phase: today's row shapes happen to
  carry every key `_corpus_row_html` reads. A future row shape (a new
  `conserve` field renamed, a `None` where a dict was assumed, a legacy row
  from an older schema) would hit this exact seam.

  Fixed with the render-phase twin of round 1's gather-phase mechanism:
    `_corpus_render_row_or_error(r)`   tries `_corpus_row_html(r)`; on ANY
                        exception returns `_corpus_row_render_error_html(r,
                        exc)` instead and reports `render_failed=True`.
    `_corpus_row_render_error_html(r, exc)`   a row that was gathered but
                        cannot be rendered must not vanish — it is shown,
                        with the bibkey (defensively re-read, never
                        trusting `r` is even a dict), the exception's
                        type+message, and a sentence making clear the row
                        WAS gathered and only rendering it failed. Built
                        with exactly 8 `<td>` cells — one per column the
                        header declares, `1 + 1 + 6` — never a `colspan`
                        cell: a `colspan` merges columns VISUALLY but does
                        NOT create the extra DOM children the inline JS
                        sorter's `a.children[idx]` reads, so a colspan row
                        would leave every later column with no cell to sort
                        on THIS row's account — the same "it happens not to
                        matter today" shape being fixed, recurring one line
                        lower if not watched for.
  Also hardened the sort script itself: `a.children[idx]`/`b.children[idx]`
  are now null-checked before `.getAttribute` is called, so even a FUTURE
  row shape with fewer than 8 cells cannot throw inside the click handler
  and silently break sorting for the whole page.

  The page's summary paragraph also used to fold `errored` into
  `unavailable` (minor finding alongside this one) — the headline sentence
  carried no separate count for it even though the per-row cell text and
  the CLI's own prose both did. Fixed: the summary now reads "...N
  conservation unavailable (no/stale/oversized model), and M could not be
  read at all (errored — its own number, never folded into
  'unavailable')."; when `n_render_failed > 0` a further sentence names it
  too.

  VERIFIED FAILING PRE-FIX, THEN FIXED (same convention as every other
  regression test in this task): reverted just `_render_corpus_index_html`'s
  guard (via `git stash` on `commands.py` alone, test file kept) and ran
  `test_render_phase_isolates_a_single_row_failure` — it failed with the
  bare `RuntimeError` propagating out of `cmd_corpusstatus`, through
  `_write_corpus_index_html`, through `_render_corpus_index_html`'s
  generator expression — the exact call chain the finding named. Restored
  the fix; the same test then passes, and the malformed row's `<tr>` has
  exactly 8 `<td>` cells like every other row, confirmed by direct count.

  Re-ran the full corpus AGAIN after this fix (2,931 documents, 415.7s
  command-reported / 6:56.07 wall clock, exit 0): the state counts are
  BYTE-IDENTICAL to fix round 1's (34/1,196/107/1,594) — asserted, not
  merely restated, in `out/653c/script.py` (`unchanged_from_fix_round_1`).
  0 rows on this real run needed either the gather-phase or the
  render-phase error path (`errored_rows_on_this_real_run: 0`,
  `render_failed_rows_on_this_real_run: 0`, checked against the real
  `>errored — ` cell-text marker, NOT a bare substring search — a bare
  search for "errored" false-positives on the summary paragraph's own
  explanatory prose, which names the word without any row being in that
  state).

THE TOTAL-FUNCTION FIX (fix round 3 — the third instance of the SAME
defect, and the reason this is a design fix, not another wrapped call site)
  Round 2's `_corpus_render_row_or_error` protects `_corpus_row_html`'s
  call into `_corpus_row_state`. But `_render_corpus_index_html` ALSO
  calls `_corpus_state_counts(rows)` — a SECOND, independent traversal of
  the SAME `rows` list, immediately after the (correctly protected) render
  loop, that calls the identical `_corpus_row_state` with no guard at all.
  A row with an incomplete `conserve.counts` dict (missing `unclaimed`/
  `doubly_claimed` — a plausible legacy or partially-written shape, not a
  contrived one) makes `c["unclaimed"] > 0 or c["doubly_claimed"] > 0`
  raise `KeyError` inside `_corpus_row_state`. Reproduced live, three ways,
  on the identical row: `_corpus_render_row_or_error` (round 2's fix)
  correctly survives it; `_corpus_state_counts` on its own raises;
  `_render_corpus_index_html` — the function that actually matters —
  raises the SAME `KeyError` and returns no HTML at all. Since
  `_write_corpus_index_html` calls `_render_corpus_index_html` strictly
  BEFORE `write_bytes`, this voids the whole `index.html` write for all
  2,931 rows, including every one the round-2 fix's row loop had ALREADY
  rendered successfully — finding 3's exact catastrophic shape, one call
  site further down the same function. `cmd_corpusstatus`'s own separate
  `counts = _corpus_state_counts(rows)` call is a FOURTH latent instance
  of the identical unprotected pattern, moot only because the crash three
  lines earlier (via `_write_corpus_index_html`) fires first.

  Three rounds finding the same failure one call site later is the
  reviewer's own diagnosis, taken as the standard rather than argued
  against: the defect was never "a caller forgot to guard," it was that
  `_corpus_row_state` is a PARTIAL function — it raises on row shapes the
  pipeline can legitimately produce — with more than one caller, and each
  round's fix guessed at which caller needed wrapping next while leaving
  the function itself exactly as fallible. Guessing a third (or fourth)
  call site to wrap was rejected in favour of making the function TOTAL:
  `_corpus_row_state`'s entire body is now inside its own try/except, and
  ANY exception — `KeyError`, `AttributeError` on `r=None`, a future
  `TypeError` from a shape nobody has imagined yet — returns `'errored'`
  rather than propagating. This makes EVERY caller safe by construction,
  including the two the review found, the fourth it named as latent, and
  any written later; the two existing wrappers
  (`_corpus_row_or_error`/`_corpus_render_row_or_error`) are now
  belt-and-braces against `_corpus_row_html`'s/`_corpus_status_row`'s OWN
  other field accesses (e.g. `cons = r["conserve"]` at the very top of
  `_corpus_row_html`, evaluated BEFORE `_corpus_row_state` is even called),
  not the last line of defence for THIS specific defect any more.

  `{"conserve": {}}` (present but empty — no `available` key at all) is
  deliberately NOT one of the "must become errored" cases: `cons.get(
  "available")` on an empty dict is `None`, falsy, and the function
  already read that correctly as `'unavailable'` before this round — a
  sensible default, not a bug. The total-function change must not turn an
  already-correct answer into `'errored'` just because a `try` now wraps
  it; a dedicated test pins this down
  (`test_corpus_row_state_missing_available_key_is_unavailable_not_
  errored`) so a future, looser rewrite of the guard cannot regress it
  silently.

  TESTED DIRECTLY ON THE MALFORMED SHAPES (the review's own instruction:
  "test the function directly ... not only through its callers" — every
  existing test before this round reached `_corpus_row_state`'s failure
  mode only through `_corpus_row_html`, which happens to be called with
  well-formed rows, so the second, unwrapped traversal was untested as
  well as unfixed):
  `test_corpus_row_state_is_total_on_malformed_shapes` calls
  `_corpus_row_state` DIRECTLY on `None`, `{}`, `{"conserve": None}`,
  `{"conserve": {"available": True}}` (verdict missing), and the exact
  reproduction shape (`counts` present but missing both keys, and missing
  just `doubly_claimed` with `unclaimed=0` so the `or`'s short-circuit
  does not hide it) — every one now returns `'errored'`.
  `test_corpus_row_state_still_correct_on_every_well_formed_shape` calls
  it directly on one well-formed example of EACH of the five states
  (`ok`/`bad`/`unaudited`/a baseline-regression `bad`/`unavailable`/
  explicit `errored`), so the fix cannot have widened what the `except`
  swallows without a test noticing.
  `test_a_malformed_row_no_longer_voids_the_whole_page` is the
  integration-level regression for the EXACT chain the review reproduced:
  builds one real, well-formed row via `corpus_status_rows`, appends a
  hand-constructed malformed one (the incomplete-`counts` shape) to the
  SAME list, then calls `_corpus_state_counts`, `_render_corpus_index_html`,
  and `_write_corpus_index_html` — the three functions the review named,
  in the order they actually run — confirming none of them raises and the
  written page carries BOTH bibkeys.

  VERIFIED FAILING PRE-FIX, THEN FIXED: reverted only `_corpus_row_state`'s
  try/except (via `git stash` on `commands.py` alone, test file kept) and
  ran the two new tests above — both failed, one with the bare
  `AttributeError`/`KeyError` the fix targets, the other with the identical
  `KeyError` propagating out of `_corpus_state_counts` inside
  `_render_corpus_index_html`. Restored the fix; both pass.

  Re-ran the full corpus AGAIN after this fix (2,931 documents, 472.0s
  command-reported / 7:52.45 wall clock, exit 0): the state counts are
  BYTE-IDENTICAL to rounds 1 and 2's (34/1,196/107/1,594) — asserted, not
  merely restated, in `out/653d/script.py`
  (`unchanged_from_fix_rounds_1_and_2`), which is exactly what this round's
  fix should produce: it changes `_corpus_row_state`'s behaviour on
  MALFORMED input only, and the real corpus contains none. The same
  script also calls `_corpus_row_state` directly on five malformed shapes
  and confirms all five return `'errored'`
  (`corpus_row_state_is_total_direct_proof.none_raised: true`) — the
  evidence file proves the same claim the test suite proves, independently.

THE NUMBERS — READ OFF THE ARTEFACT `pdfdrill corpusstatus` ACTUALLY WROTE,
AFTER THE FIX (review finding 1: the whole point of this section)
  Full corpus re-run, `~/pdfdrill-library`, 2,931 documents:

    state          count   what it means
    -----          -----   -------------
    conserved         34   verdict == "conserved" (`_status_verdict`)
    NOT conserved   1,196   a real unclaimed/doubly-claimed anchor, OR a
                            genuine regression against a recorded baseline
    unaudited         107   ONLY an unbaselined violation type — zero real
                            anchor defect, no baseline row for this bibkey
    unavailable     1,594   no model (1,591) / stale (2) / oversized,
                            size-guard-skipped (1) / errored (0 — see above)
    -----          -----
    TOTAL          2,931

  Every number above is read off `~/pdfdrill-library/index.html` by CSS
  class per row (`out/653b/script.py`), cross-checked against
  `cmd_corpusstatus`'s own printed summary from the SAME run — the two
  agree exactly, because both now go through `_corpus_row_state`/
  `_corpus_state_counts`, the fix for finding 1/2.

  THE "NOT CONSERVED" BUCKET, BROKEN DOWN (this is what round 1 got wrong):
  1,196 = 5 baseline bibkeys (of the 7 in `conserve_baseline.json`) that
  read "bad" for their OWN real anchor counts, unchanged from 646/652:
    penev_A: 243 unclaimed, 29 doubly-claimed
    penev_B, SteerableWaveletFramesBasedontheRieszTransform, narcissus,
    kolbe2018hubbard: also "bad", each for its own real anchor counts
  PLUS 1,191 non-baseline bibkeys that ALSO carry a real, nonzero
  unclaimed and/or doubly-claimed anchor count — genuine, baseline-
  independent transclusion defects, not ratchet noise. Named examples
  (from the review, re-verified against this run's own `index.html`):
  `Infineon-motorcontrol_handbook-...` (31 unclaimed, 2,533 doubly-claimed),
  `0008113v1` (43 unclaimed, 41 doubly-claimed), `0110026` (186 unclaimed).

  THE SEVEN-BIBKEY BASELINE, CORRECTED: round 1's `out/653.txt` claimed all
  seven baseline bibkeys were "the real failures." Two are NOT failures at
  all — `1811.06102` and `2602.12042v1` read "conserved" (`class="ok"`) on
  this run, exactly at their recorded baseline with zero unclaimed/doubly-
  claimed anchors. Only 5 of the 7 are in the "bad" bucket.

  THE 107 "unaudited" ROWS are a genuinely different population from the
  1,196 "bad" ones: zero measured anchor defect, the sole issue an
  unbaselined violation type. Sample (first 10 alphabetically):
  1405.0030vG, 1605.0232v1, 1609.03457, 1611.09182, 1703.01192, 1707.08872,
  1708.00006, 1710.04811, 1711.04607, 1711.10105 — full list in
  `~/pdfdrill-library/index.html` (sort by the "conserved" column) or
  `out/653b/report.json`.

  DO NOT read "1,303 documents fail conservation" (1,196 + 107) as one
  homogeneous group, as round 1 did — that IS the mistake this fix round
  corrects. 1,196 are measurably broken; 107 have simply never been
  reviewed.

  `--only-published`: 21 named documents, 34.8s. All 21 read "bad" (0 ok, 0
  unaudited, 0 unavailable) — every one of the 21 currently curated
  documents carries a real anchor defect of its own. 1 entry in
  `documents.json` was skipped (finding 10): `pdf-dc99cf88` has a null path
  recorded (a pre-existing data-quality fact about the published set, not
  something this task caused) — reported by `cmd_corpusstatus`'s own prose
  now ("skipped from the published set (1): ..."), not silently dropped
  from the row count as round 1 did.

SCALE — UNCHANGED FROM ROUND 1, RE-MEASURED
  Same two guards as before (500 MB hard size guard; `--only-published` for
  a cheap subset check), confirmed correct by the review (finding 6). This
  round's full-corpus run: 2,931 documents, 444.0s command-reported /
  7:24.43 wall clock (`/usr/bin/time -v`), 99% CPU, 908 MB peak RSS, exit
  status 0 — essentially identical to round 0's 466.2s/7:46.66/907 MB
  (normal run-to-run variance on the same machine; the exception-isolation
  try/except adds no measurable overhead on a corpus with 0 real
  exceptions to catch).

THE SURROGATE BUG — UNCHANGED, RE-CONFIRMED (review finding 7)
  `_write_corpus_index_html` still writes bytes with
  `html.encode("utf-8", "replace")`. The review reproduced the pre-fix
  `UnicodeEncodeError` independently against the actual committed code with
  the fix reverted, and confirmed the regression test
  (`test_surrogate_escaped_filename_does_not_crash_the_index_write`) fails
  the same way pre-fix and passes post-fix. Not touched this round.

TESTS
  `tests/test_corpusstatus.py`, 23 tests (round 0 had 13; round 1 net +4;
  round 2 net +2; round 3 net +4, all new, none replaced):
    - NEW (round 3): `test_corpus_row_state_is_total_on_malformed_shapes` —
      calls `_corpus_row_state` DIRECTLY (not through a caller) on `None`,
      `{}`, `{"conserve": None}`, a `conserve` dict missing `verdict`, and
      the exact reproduction shape (`counts` present but missing
      `unclaimed`/`doubly_claimed`, with `unclaimed=0` set explicitly so
      the `or`'s short-circuit cannot hide the missing key) — every one
      returns `'errored'`, none raises.
    - NEW (round 3): `test_corpus_row_state_missing_available_key_is_
      unavailable_not_errored` — pins down that `{"conserve": {}}` was
      NEVER a raising shape (falsy `available` already correctly meant
      `'unavailable'`) and must stay that way, not be swept into `'errored'`
      by the new blanket `try`.
    - NEW (round 3): `test_corpus_row_state_still_correct_on_every_well_
      formed_shape` — calls the function directly on one well-formed
      example of each of the five real states, so the fix cannot have
      quietly widened what the `except` swallows.
    - NEW (round 3): `test_a_malformed_row_no_longer_voids_the_whole_page`
      — the integration-level regression for the EXACT chain the review
      reproduced: one real row plus one hand-built malformed row, through
      `_corpus_state_counts`, `_render_corpus_index_html`, and
      `_write_corpus_index_html` in that order; confirmed FAILING pre-fix
      (`git stash` on `commands.py` alone) with the identical `KeyError`
      from `_corpus_state_counts` inside `_render_corpus_index_html`,
      passing post-fix with both bibkeys on the written page.
    - NEW (round 2): `test_render_phase_isolates_a_single_row_failure` —
      monkeypatches `_corpus_row_html` to raise for exactly one of three
      documents; confirms `cmd_corpusstatus` does NOT raise, all 3 rows
      appear on the page (nothing vanished), the failed row's cell reads
      "could not be rendered on this page" with the exception's message,
      and its `<tr>` still carries exactly 8 `<td>` cells (sortability
      preserved). Confirmed FAILING pre-fix (`git stash` on `commands.py`
      alone): the bare `RuntimeError` propagated through
      `cmd_corpusstatus` -> `_write_corpus_index_html` ->
      `_render_corpus_index_html`'s generator expression, exactly the call
      chain the finding named.
    - NEW (round 2): `test_page_summary_gives_errored_its_own_number` — a
      library with one gather-phase error among 3 documents; the page's
      summary paragraph (whitespace-collapsed before the substring check,
      since the source wraps mid-sentence) names BOTH "1 conservation
      unavailable" AND "1 could not be read at all (errored" — two
      numbers, not one folded into the other.
    - the basic shape, `--only-published`, the size guard, the CWD trap
      (happy path), artefacts-present, the sortable page, "no such library
      directory", the surrogate regression, the not-`cmd_`-prefixed write
      helper, CLI flag parsing — all UNCHANGED from round 1, still passing.
    - REPLACED: `test_not_conserved_documents_are_named_in_the_report`
      (asserted the now-corrected-away behaviour: an unaudited-only
      document counted as "NOT conserved"). Split into:
        `test_real_anchor_defect_is_named_not_conserved_in_the_report` — a
          document with a genuinely unclaimed anchor (no violation type at
          all) reads "bad"/"NOT conserved: 1", never "unaudited".
        `test_unaudited_violation_only_document_is_not_the_same_bucket` — a
          document with only an unbaselined Citation (zero unclaimed,
          zero doubly-claimed) reads "unaudited: 1", never "NOT conserved",
          rendered `class="unaudited"` on the page, never `class="bad"`.
        `test_a_real_anchor_defect_and_an_unaudited_document_get_
          different_states` — both shapes in ONE library, so the split
          itself is exercised, not each shape in isolation.
    - REWRITTEN: `test_cwd_restored_even_when_the_gatherer_raises` — round
      1 asserted `pytest.raises(RuntimeError)` around the whole command
      (the bug finding 3 named); now asserts the OPPOSITE — the command
      does NOT raise, the cwd is still restored, and the row reads
      "could not be read at all" in the returned prose.
    - NEW: `test_one_malformed_model_does_not_abort_the_whole_walk` — a
      REAL corrupt `model.docmodel.json` (not monkeypatched) among 3
      documents; `index.html` still gets all 3 rows, the broken one
      `errored=True`.
    - NEW: `test_only_published_reports_skipped_entries_not_silently` — a
      `documents.json` with a null-path and a nonexistent-file entry:
      `skipped == 2`, and the command's prose names the count.
    - Every test above still stubs `cmd_model` to raise if called; still
      true after this round (no new code path reaches it).

  FULL SUITE: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest
  tests/ -q -p no:cacheprovider` -> 3513 passed, 0 failed (23 in
  `tests/test_corpusstatus.py`; `test_doclock.py`, `test_skill_sync.py`,
  `test_status_html.py` also run standalone first, all green — 63 passed
  in the combined targeted run).

EVIDENCE
  Round 0 (superseded for the numeric narrative, kept for the mechanics it
  verified — the surrogate bug and its fix):
    `~/pdfdrill-library/out/653/{script.py,report.json,INSPECT.txt}`.
  Round 1 (superseded for the numeric narrative, kept for the baseline/
  non-baseline breakdown it computed):
    `~/pdfdrill-library/out/653b/{script.py,report.json,INSPECT.txt}`.
  Round 2 (superseded for the numeric narrative by round 3's confirmation —
  all three are byte-identical in every count — kept for the render-phase
  check it ran):
    `~/pdfdrill-library/out/653c/{script.py,report.json,INSPECT.txt}`.
  Round 3 (fix round — THIS report's numbers, and the total-function
  check):
    `~/pdfdrill-library/index.html` — 2,931 rows, 1,603,873 bytes, written
    by a real run of `pdfdrill corpusstatus --library ~/pdfdrill-library`
    AFTER `_corpus_row_state` was made total (2,931 documents, 472.0s /
    7:52.45 wall clock, exit 0).
    `~/pdfdrill-library/out/653d/script.py` — reads the state (CSS class)
    of every row from `index.html`, asserts the four counts are
    BYTE-IDENTICAL to rounds 1/2's (34/1,196/107/1,594 — a real assertion),
    AND calls `_corpus_row_state` directly on five malformed shapes
    (`None`, `{}`, a `None` `conserve`, a `conserve` missing `verdict`, the
    incomplete-`counts` shape), asserting none of them raises — the same
    claim the test suite makes, proven independently, against the real
    library's own module import.
    `~/pdfdrill-library/out/653d/report.json` — the full numeric record for
    this round, including the five malformed-shape results.
    `~/pdfdrill-library/out/653d/INSPECT.txt` — one path, `index.html`,
    checked to exist/be non-empty, first line (`<title>PDFDRILL corpus
    status (2931)</title>`) checked to NAME "corpus".

CONCERNS / THINGS I NOTICED BUT DID NOT FIX
  1. `iter_documents` enumerates every PDF, not only ones with a model —
     2,931 rows for 1,359 models. Unchanged from round 0; the page is
     1.6 MB, ~2,900 `<tr>`s, renders fine in the environments checked, not
     checked on a resource-constrained mobile browser.
  2. Two documents are genuinely STALE (`2503.04772v1`,
     `chung2019combinatorics`); orthogonal to this task, fix is
     `pdfdrill model` on each, never done automatically.
  3. `_corpus_model_date`'s mtime fallback remains untested against a real
     document (review finding 11, self-disclosed in round 0, "no action
     needed beyond noting it stands" — unchanged this round).
  4. The 500 MB size-guard threshold remains an undemonstrated judgment
     call in the 149 MB-4.96 GB gap (review finding 6: "accurately
     characterized as a judgment call, not a defect" — unchanged).
  5. NEW this round: `pdf-dc99cf88`'s null path in
     `~/pdfdrill-library/out/documents.json` is a genuine, pre-existing
     data-quality issue in the published set (not introduced by this
     task) that `--only-published` now surfaces (`skipped: 1`) instead of
     hiding. Not fixed here — fixing `documents.json` itself is
     `publishcheck.py`/whatever wrote that entry's territory, not
     `corpusstatus`'s.
  6. The "unaudited" vs "bad" split depends entirely on
     `conserve_baseline.json` covering only 7 bibkeys (656's own scope
     decision, unchanged). If that baseline is ever extended, some of the
     1,191 non-baseline "bad" rows and some of the 107 "unaudited" rows
     will move between buckets — expected, not a defect in this task's
     classification logic, which reads the baseline file, not a cached
     copy of it.
  7. `_write_corpus_index_html` itself still has no try/except around its
     own `write_bytes` call (noted round 2, unchanged). Deliberately left
     unguarded — a failed WRITE of the whole index (disk full, permissions)
     is a different class of failure than one bad ROW among thousands, and
     swallowing it would hide a real operational problem with no row-level
     substitute to show instead; the fix rounds are scoped to per-row
     isolation, which is what every blocking finding so far (3, round 2's,
     and round 3's) actually asked for, not "no exception anywhere in this
     command can ever surface."
  8. NEW this round, said plainly rather than left implicit: THREE fix
     rounds found the identical defect (`_corpus_row_state` raising on a
     malformed row) at three successive call sites before it was fixed at
     the function itself. That is a real cost of the earlier rounds' own
     approach, not just this round's — each treated the symptom (a
     specific unguarded caller) as the defect, when the actual defect was
     a partial function with more than one caller. Named here so a future
     reader does not read rounds 1/2's fixes as "the pattern that worked
     eventually" — they were two guesses that each left one caller
     exposed; round 3's total-function fix is the one that closes the
     class, not just this instance of it. `_corpus_status_row` (the
     GATHER-side function 656/652's own `_status_conserve_ledger` chain
     ultimately feeds into) was checked for the same "partial function,
     multiple callers" shape and found to have exactly ONE caller
     (`_corpus_row_or_error`) — so it was not converted to total-by-
     construction, on the judgment that a single call site does not carry
     the same risk a function with 2+ callers does; if a second caller of
     `_corpus_status_row` is ever added, this judgment should be revisited
     rather than assumed to still hold.
