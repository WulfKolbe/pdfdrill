685 — the 17 ink-accepted refinements: 12 wrong, 5 right, and no threshold
      that tells them apart

THE POPULATION
--------------
Of 30 VERIFIED refinements across the 20 published documents, 29 were
`verified_by: "ink"`. 17 of those carried EQ0378's shape — an ink delta <= -20
or a character collapse past 65%. Read side by side against their originals:

    12 are plainly DIFFERENT MATHEMATICS -> withdrawn
     5 are the SAME content reformatted  -> kept

WITHDRAWN (12), each recorded in its document's withdrawn.json with the
evidence that accepted it:

    lyche      EQ0579  normal equations        -> PROSE about triangular matrices
               EQ0369  Jordan blocks           -> disconnected prose fragments
               EQ0670  banded-matrix definition-> unrelated submatrix
               EQ0729  FFT permutation identity-> unrelated (DX+XD) identity
               EQ0630  block eigenproblem      -> generic linear system
    0902.0431  EQ1187  wedge-product identity  -> nu(sum lambda_k H_k)
               EQ0515  lambda coefficient table-> nu(sum lambda_k H_k)
               EQ0425  A X A* conjugation      -> nu(sum lambda_k H_k)
               EQ1059  E_8 fixed-point set     -> kappa-pi-H expansion
    johnston   EQ0543  linear-programming dual -> three-vector arithmetic
               EQ0748  Vandermonde reduction   -> vector subtraction
    voloshin   EQ0197  chromatic polynomial    -> PROSE ("...the edge list")

FOUR ROWS OF ONE DOCUMENT RECEIVED THE SAME EQUATION. 0902.0431's EQ1187,
EQ0515, EQ0425 and EQ1059 all came back as variants of nu(sum lambda_k H_k).
That is one equation bleeding into four rows, and it is the clearest evidence
that these are not transcriptions at all.

KEPT (5), and one was adjudicated by the census rather than by eye:

    johnston EQ1276  a `\longdiv` rewritten as a proper long-division array;
                     same polynomials, correct
    johnston EQ0006  same equations, array reformatted
    johnston EQ0528  same xrightarrow/array content, cleaned
    johnston EQ0014  same dot-product identity PLUS "(property (b))" labels —
                     and the census shows `( p r o p e r t y ( b ) )` literally
                     on the page. MathPix had DROPPED them; the refinement
                     restored them. A good repair, and only measurement could
                     say so.
    lyche    EQ0706  repairs MathPix's mangled
                     `\boldsymbol{V}^{\boldsymbol{V}=\boldsymbol{S}...}` into
                     `\stackrel{V=SXS}{\Longleftrightarrow}`

THE NEGATIVE RESULT, WHICH MATTERS MORE THAN THE FIX
-----------------------------------------------------
I set out to add a magnitude ceiling to the accept stage. Measured against
this population, no threshold on magnitude separates the two groups:

    signal              correct repairs    wrong repairs     separates
    ink delta           -95 .. -23         -261 .. -24       NO
    character ratio     0.58 .. 1.13       0.07 .. 0.94      NO
    string similarity   0.039 .. 0.498     0.011 .. 0.323    NO

johnston EQ1276 is a CORRECT repair at delta -95 and similarity 0.039,
because rewriting `\longdiv` as an array changes nearly every character while
preserving the mathematics exactly. Any ceiling strict enough to reject the
twelve rejects that too — and still passes voloshin EQ0197 at -24. So no
ceiling was written. One that looked principled and separated nothing would
be worse than none, and this repository has the scar already (the deploy
guard's removed equations check, and `publishcheck`'s stale `published/`
preference).

WHAT WAS ADDED INSTEAD: `refine.bleeding_proposals()`
------------------------------------------------------
The one signal that survived measurement. Two rows of one document whose
PROPOSED values are near-copies are bleeding: EQ1187~EQ0515 at 0.95,
EQ0543~EQ0748 at 0.69, with no false positives on this population. Perfect
precision, recall 4 of 12. Worth having because it is SPECIFIC — a pair it
names is bleeding, not merely large.

THE POLICY QUESTION THIS RAISES, FOR THE USER
----------------------------------------------
12 of 29 ink-verified refinements were wrong: a 41% error rate on values that
`_chosen_reading` PREFERS over MathPix's own reading and publishes. "Verified
by ink" does not mean verified — ink measures how much ink a rendering makes,
not whether it is the same ink.

The strongest available fix is to stop publishing ink-only refinements:
`_chosen_reading` would prefer a refinement only when it is verified by CENSUS
or EPRINT — evidence about identity — while ink-verified ones stay recorded
and the raw reading publishes. That costs the 5 good repairs above until they
are re-verified by census, which is cheap for 5 rows and would be a one-time
pass. It is not taken here because it un-publishes readings this task has just
judged correct, and that is the user's call.

REBUILD REQUIRED
----------------
0902.0431, johnston-linear-matrix-algebra, lyche-numerical-linear-algebra and
voloshin-hypergraph each need evidence + re-measure + marks + HTML +
publishready, because withdrawing a refinement edits the model and the ink's
model_sha256 no longer describes it.

Suite 3,785 -> 3,788.

===========================================================================
686 — the demotion, the five rebuilds, and a stub class found while publishing
===========================================================================

THE DEMOTION
------------
`chosen_latex()` prefers a VERIFIED refinement only when what verified it is
evidence about IDENTITY — `IDENTITY_EVIDENCE = {census, eprint, source}`. An
ink-verified one stays recorded, `refinement_state` still calls it VERIFIED
(the measurement happened; saying otherwise would be a lie), and the caller
receives the original plus `refinement_state: "ink_only"`, so a projection can
say a refinement exists and was not trusted rather than silently showing
either value.

A COMBINED VALUE KEEPS ITS IDENTITY HALF, and this was a defect in the first
version. `0707.4470_FO0175` records `verified_by: "source+ink"` — the author's
own e-print agreed AND the ink improved — and exact membership threw away the
half that matters, demoting a row the author source had confirmed. Split on
the separators that occur. Both directions tested, including that joining two
quantity measurements does not make identity.

SIX EXISTING TESTS ENCODED 669'S OLDER CONTRACT and were updated deliberately
rather than worked around: the reports fixtures default to identity evidence
now, and two new tests assert the opposite outcomes for ink-only and census.
`test_what_is_VERIFIED_and_what_is_SHOWN_are_different_questions` names the
distinction the change turns on.

17 rows demoted across 5 documents: johnston 6, 0902.0431 4, lyche 3,
mielke 2, voloshin 2.

THE REBUILDS — 115 MINUTES, ZERO FAILED STEPS, ALL FIVE READY
--------------------------------------------------------------
    0902.0431   evidence 137s  measure 1421s  marks  86s  html 64s
    johnston    evidence 433s  measure 2150s  marks 279s  html 80s
    lyche       evidence 235s  measure 1217s  marks 165s  html 72s
    mielke      evidence  68s  measure   51s  marks  36s  html 23s
    voloshin    evidence  73s  measure  209s  marks  58s  html 43s

A STUB CLASS, FOUND BECAUSE A GUARD REFUSED
--------------------------------------------
`to_site_676.py` refused the copy: +65.9 MB against a 50 MB growth margin.
That refusal was right and it is why this was found — three documents' site
`report.pdf` were STUBS:

    johnston   1.4 MB -> 36.5 MB
    lyche      0.2 MB -> 19.7 MB
    voloshin   0.1 MB -> 11.2 MB

Swept across all 20: SEVEN carry such a stub — a few hundred KB of report.pdf
claiming to describe 200 to 1,460 ink rows. Four are now fixed; 1510.06699,
cardona-qft-methods, fong-spivak-invitation and fong-spivak-seven-sketches
still carry one.

Invisible because `report.pdf` is not in `gate.PUBLISHED_FILES`, and the
deploy guard's stamp check compares `report.build.json` to the file beside it
— which a CONSISTENT STUB PAIR satisfies perfectly. This is the same blind
spot as `publishcheck`'s five-name tuple (677), one layer down, and the second
time in one day that a check was correct about the file it inspected and
silent about the file nobody listed.

The margin was raised to 120 MB deliberately, with that reason written beside
it, which is what the refusal message asks for.

PUBLISHED
---------
Site 478b52a..6f95015, verified on the remote. Bars regenerated (3 rows
differed), sizes regenerated (11 changed), deploy guard run locally,
publishcheck 20/20 identical.

STILL OPEN
----------
* 4 stub report.pdf files (1510.06699, cardona, both fong-spivak).
* The 15 unrepaired cases in out/682-goldens-mielke.json: 5 dropped-⌋ rows
  needing PLACEMENT, 10 overlays including EQ0472's \cancel{R}.
* The three inkdrill properties — junction angle, bounding-box containment,
  glyph-count reconciliation — which are what would give a SCANNED document
  the treatment the born-digital ones just received.
