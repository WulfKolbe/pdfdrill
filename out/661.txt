661 — port the display->in-math environment map
==================================================

"start 661 and port the env map." Branch eqblobs-and-gzip-tex, base commit
f25ca1cc, clean.

THE PORT — taken as-is, not changed
------------------------------------
scratchpad/full_report.salvaged.py's `_ENV_MAP` / `to_inline_env()`
(recovered from the lost session, own reasoning in its own comments) are
ported VERBATIM into `src/pdfdrill/report_tex.py`, right before `renderable()`
(same neighbourhood as `_MATH_ENVS`, per out/657 item 2's ownership call):

    _ENV_MAP = (("align", "aligned"), ("alignat", "aligned"),
                ("flalign", "aligned"), ("eqnarray", "aligned"),
                ("gather", "gathered"), ("multline", "gathered"),
                ("split", "aligned"))

    def to_inline_env(latex: str) -> str:
        out = latex or ""
        for src, dst in _ENV_MAP:
            out = re.sub(r"\begin\{%s\*?\}" % src, r"\begin{%s}" % dst, out)
            out = re.sub(r"\end\{%s\*?\}" % src, r"\end{%s}" % dst, out)
        out = re.sub(r"\(?:begin|end)\{equation\*?\}", "", out)
        return out.strip()

No change to the regex, the pairs, or the order. CORRECTION (fix round 1,
finding 4): the original claim here said a `-> str` type hint was added to
`to_inline_env`'s signature — false, checked directly against
`scratchpad/full_report.salvaged.py:61`, which already reads
`def to_inline_env(latex: str) -> str:`. Nothing was added to the
signature; only the docstring's citations were added, matching this
module's style — 659's precedent (Rule 3: reuse a measured artefact over
re-deriving a look-alike) argues for taking the code verbatim, and
out/657 confirms no equivalent exists anywhere on this path to conflict
with it.

CORRECTION (fix round 1, finding 5): the ported comment on `split`
inside `_ENV_MAP` originally read `\begin{split}` (one backslash) where
the salvaged original (`scratchpad/full_report.salvaged.py:56`) has
`\\begin{split}` (two, confirmed with `cat -A`) — an undisclosed,
unintentional deviation on comment TEXT (no behavioural effect; the code
itself, verified separately, was already identical). Restored to match
the salvaged original exactly.

THE INVENTORY'S OTHER FINDING, checked directly
-------------------------------------------------
out/657 item 2 named two "kin" and asked me to say why neither is the
right home:
  - `latex_source.py:920` `_ALIGNED_ENVS`/`_clean_eq` — wraps a body in
    `aligned` uniformly (never `gathered`, never covers `split`), and only
    for the LaTeX/KaTeX source-projector's own already-tagged display
    items. Not this gate's population, not this gate's map.
  - `docops/projectors/latex_pipeline.py:111-127` `_DISPLAY_ENV`/
    `_inline_safe` STRIPS wholesale for the `\Expr{i}` speech path
    (la2speech) — deliberately destructive, the opposite operation from
    what this task asks for (map, not strip).
Neither is adopted; report_tex.py remains the right home, confirmed by
reading both in full.

THE DESIGN QUESTION — where the transform belongs
----------------------------------------------------
`renderable()` is measured, tested (60+ existing tests depend on its exact
behaviour), and its whole contract is "judge this value." Every rewrite it
already performs (whitespace collapse, the 634 boldsymbol/operatorname
swap, furniture-drop, delimiter-glue repair) is a SAFETY normalization —
none of them changes which environment a reader would say the value is.
Mapping `align` to `aligned` is a different kind of move: it changes the
value's own identity, not just its safety. Bolting it into `renderable()`
would blur that line for every future reader of the function's docstring,
which currently earns its claim ("safe to put inside $...$, or refuse")
honestly.

So the map lives OUTSIDE the gate, as `to_inline_env()`, and a new function
composes the two:

    def display_safe(latex: str) -> str:
        return renderable(to_inline_env(latex)) if latex else ""

`renderable()` itself gained one paragraph saying explicitly that it does
NOT call `to_inline_env()`, and why — so the boundary is stated where a
reader of the gate would look for it, not only where the map is defined.

Every call site in report_tex.py that previously called `renderable(latex)`
to decide "does this row render" or "what goes in the Rendered cell" was
switched to `display_safe(latex)` — seven sites: `row()` (the main
Display-equations/Inline-formulas builder), `unresolved_formulas()`,
`refused_for_align_only()`, `findings_rows()`, `findings_tex()`'s local
`rendered()` closure, `b_tex()`'s per-row loop, and the Tables section's
per-row loop. This matters beyond tidiness: `unresolved_formulas()`'s own
docstring states an invariant — "qualifies... exactly the row whose
Rendered cell says (not rendered)" — that breaks the moment the actual
Rendered cell (in `row()`) and this classifier disagree on what "renders"
means. Routing every one of the seven through the same `display_safe()`
keeps that invariant true BY CONSTRUCTION rather than by the two
implementations happening to agree today.

Two call sites were deliberately NOT switched, and are named as such in
`display_safe()`'s own docstring:
  - `region_render()` (image-region LaTeX, DIA/PIC) — a different
    function's concern, gated by its own `_MATH_ENVS` allowlist. Mapping
    there BEFORE that allowlist check would let `align`/`gather`/etc.
    through it as a side effect — a scope decision for that function, not
    this task.
  - `listing_cell()`'s `\(...\)` spans inside an `lstlisting` body — a much
    narrower, rarer shape (formulas inside a code listing); not measured
    to carry a display environment anywhere in the corpus, left unchanged.

THE CONSEQUENCE — which form is published
--------------------------------------------
DECISION: the "LaTeX source" column shows `latex` UNTRANSFORMED — MathPix's
(or the e-print source's) own reading, exactly as read. The "Rendered"
column shows the SAME reading with `to_inline_env()`'s map applied, via
`display_safe()`. The two columns are allowed to disagree, by construction,
on the population where the map fires.

ARGUMENT. The "LaTeX source" column's entire reason to exist — stated
throughout this file's own history (233's whole point: "what did the OCR
read" vs "what does this document say" are two different questions this
report exists to keep apart) — is fidelity to the reading. A silent
substitution of one environment name for another, however rendering-
equivalent, is exactly the kind of edit that column exists to NOT show.
The alternative (map the source column too) would mean the report no
longer has ANY column that states what MathPix returned for these rows —
weakening the report's core claim for the sake of two matching columns.

This is not a new posture for this file: `renderable()`'s OWN existing
rewrites (634's boldsymbol/operatorname swap, furniture-drop, delimiter-
glue repair) already make the Rendered cell diverge from the LaTeX-source
cell for other rows, and no prior task treated that as a defect requiring
a matching source-column edit — the source column's job has always been
"the reading," the render column's job has always been "what can safely
be typeset from it." This fix keeps the same division; it does not
invent one.

`row()`, `findings_tex()`'s `rendered()`, `b_tex()`'s loop, and the Tables
loop were each given a short comment stating the source cell is built from
the UNCHANGED `latex`. `b_tex()`'s docstring ("The three columns: LaTeX
source, its rendering, the image") was extended to say so explicitly,
because B.pdf carries no legend at all (B_WIDTHS' own docstring: "Three
columns, and nothing else") and has nowhere else to say it.

For every OTHER table (row()/findings_tex/the Tables section, which share
`table_open()`/`legend_foot()`), a new legend line was added and is now
ALWAYS printed (matching 181's own precedent for LEGEND_CONF: "the
confidence bands ALWAYS apply, so the legend is always printed" — the
LaTeX-source/Rendered columns are present on every table this legend
serves, so the key that says they may differ applies unconditionally):

    LaTeX source: MathPix's reading, unchanged; Rendered: the same
    reading, with a display environment (align/alignat/flalign/eqnarray/
    gather/multline/split) mapped to its in-math form so it can typeset
    in this cell — the two may legitimately differ

TWO TRAPS, both checked directly
------------------------------------
1. MAP, NOT UNWRAP. `to_inline_env()` renames the environment; it never
   deletes the `\begin{X}...\end{X}` wrapper. Verified two ways in
   `test_to_inline_env_maps_not_unwraps`: the mapped output still strips to
   NOTHING under `_strip_innermost_envs` (no residue at all — the wrapper
   is still there to be stripped) and `renderable()` accepts it unchanged;
   an UNWRAP built by the same test (deleting only the `\begin`/`\end`
   tokens, leaving the body bare) is shown to fail `has_bare_align_marker`
   after the same strip — the exact condition 659 spent a task removing
   from the opposite direction (an outer environment stripped while ITS OWN
   bare `\\`/`&` were exposed). This task does not reintroduce that shape
   from the inside.

2. RULE 17. Every fixture in the new tests starts from a genuine DISPLAY
   environment (`\begin{align}`, `\begin{split}`, etc.) with a bare `&`/`\\`
   inside it — never `aligned`/`gathered` already. A fixture starting
   in-math would pass against the UNPORTED code (nothing refuses `aligned`
   today) and would prove nothing about this map. Checked explicitly for
   `split`: `test_split_alone_reaches_renderable_but_is_not_yet_
   typesettable` confirms the RAW value passes `renderable()` unrefused
   (the auditor's own framing: this is not an over-refusal bug), and the
   live-compile test below shows what actually breaks it.

MEASURED — the auditor's named symptom, reproduced and fixed, live
-----------------------------------------------------------------------
Directly against THIS repo's own `report_tex.preamble()`, in a real
longtable cell, `$\displaystyle \begin{align}...\end{align}$` (row()'s
own wrapping) raises:

    ! Package amsmath Error: \begin{align} allowed only in paragraph mode.

and `$\displaystyle \begin{split}...\end{split}$` — the auditor's named
symptom — raises:

    ! Package amsmath Error: \begin{split} won't work here.

Both are LIVE COMPILE ERRORS, not gate refusals: `renderable()` on the RAW
value returns it UNCHANGED for every one of the seven names (verified
directly for all seven in a REPL before writing any fix) — the map's job
is not to stop an over-refusal, it is to stop xelatex from being handed
something that cannot typeset where it is placed. After the map,
`$\displaystyle \begin{aligned}...\end{aligned}$` and the `gathered`
equivalent both compile with 0 errors in the same cell
(`test_split_alone_no_longer_won_t_work_here`, a real xelatex compile,
gated on `shutil.which("xelatex")`).

CORPUS SEARCH — why 1911.08029, not 0902.0431
--------------------------------------------------
0902.0431 (this task's default example document, and the population 657/
659 already measured) was checked FIRST, as the brief anticipated it might
need to be replaced. `grep -rl` over ~/pdfdrill-library's model.docmodel.json
files for the seven env names found 0902.0431 among 49 candidates — but a
TEXT hit is not a REAL hit: the `\begin{eqnarray*}` in that file sits inside
an alternate "latex_candidate" realization (a cross-reference text blob
from a different stream), never in the Equation's OWN `latex` prop that
`report_tex.rows_for`/`row()` actually reads. Checked directly via DocGraph:
0902.0431 has 1,219 Equation objects and 0 of them carry any of the seven
names in `props["latex"]"` — those three occurrences resolve to
`\begin{aligned}` in the actual reading. So 0902.0431 is confirmed to carry
NO display environment in any cell, exactly the brief's contingency, and a
different document was chosen.

Re-checking all 49 grep candidates through DocGraph (script and full output
saved at ~/pdfdrill-library/out/661/, corpus-level per 525's convention —
no single document): 9 of 49 carry a genuine `Equation.latex` hit, and
EVERY ONE of the nine is `split` — zero real occurrences of align/alignat/
flalign/eqnarray/gather/multline were found anywhere in this library's
Equation.latex props. This is a real, reportable finding, not an artefact
of the search: MathPix's own extraction (and whatever normalization runs
between it and `props["latex"]`) already appears to fold align/gather/
eqnarray/alignat/flalign/multline into their in-math forms upstream of
this gate for every document sampled — but `split` slips through
unnormalized. The fix stays correct and general for all seven regardless
(and is defensive for any future source that does emit them raw — an
e-print's own LaTeX, `injectlatex`'s path, is exactly such a source), but
the CURRENT corpus's measured population is `split` alone.

CHOSEN: 1911.08029 ("A mixed finite element method with piecewise linear
elements for the biharmonic equation on surfaces", built via `model` from
its own arXiv e-print LaTeX source — `injectlatex`'s path, not MathPix OCR).
94 Equation objects, 48 of them (51%) carry `\begin{split}...\end{split}`
directly in `props["latex"]` — by far the largest population of the nine
hit-documents (next largest: 2304.14263 at 59 of 372, a smaller fraction).

MEASURED, 1911.08029, all 94 Equation objects — FIX ROUND 0, SUPERSEDED PATH
--------------------------------------------------------------------------------
CORRECTION, fix round 1 (blocking finding 3): everything in this section
was verified through `report_tex.row()`/`compile_fixpoint()` only — the
`reporttex` command. Per `docs/HANDOVER.md:25-42` this is RETIRED from
the site / "superseded for reading, kept as the ink chain's measure
build"; the artefact all 21 published documents actually ship is built by
`pdfdrill evidence`/`pdfdrill residuals` through `src/pdfdrill/reports/`,
a SEPARATE code path this section never touched. The claim below to have
verified "through the production ... path" is true of the superseded path
and false of the one that ships — kept here UNCHANGED as a record of what
fix round 0 actually did (and did not do), with the real published-path
measurement in its own section below.

Script: ~/pdfdrill-library/1911.08029/out/661/661_measure_script.py.

Gate state (renderable/display_safe refused vs not) — UNCHANGED, as
predicted above: 94 Equation objects; gate-refused BEFORE: 0; AFTER: 0.
This population was never gate-refused; the defect this task fixes is not
in that count.

Compile state — the population that actually matters here, measured by
compiling each of the 48 `split` cells TWICE (raw and mapped) in its own
private longtable, real preamble:
  - BEFORE (raw `split`): 48 of 48 raise EXACTLY 1 compile error each,
    "Package amsmath Error: \begin{split} won't work here" (identical
    message on every one — same defect, same population).
  - AFTER (`to_inline_env`-mapped): 0 of 48 raise any error.

Then verified through the REAL PRODUCTION CODE PATH, not the measurement
script's own cell wrapper — `report_tex.row()` itself, compiled via
`report_tex.compile_fixpoint()` (the same fixpoint `reporttex` runs), first
for a 10-row sample then for all 48:
  - 10-row sample, POST-fix (~/pdfdrill-library/1911.08029/out/661/
    probe661.tex/.log/.pdf): compile_fixpoint -> (3 pages, 0 errors,
    0 demoted). All 10 render.
  - 10-row sample, PRE-fix (`display_safe` monkeypatched back to
    `renderable` only — everything else, including `renderable()` and
    `to_inline_env()` itself, untouched — probe661_before.*): (3 pages,
    0 RESIDUAL errors, 10 demoted). The 0 is not "0 errors" in the sense
    of "nothing wrong" — it is 0 errors ONLY because `compile_fixpoint`
    demoted every one of the 10 to "(not rendered)" before the count was
    taken. This is the actual, concrete effect of the pre-fix state on a
    real report build: a `split` row was never garbled or left to crash
    the document, it was silently downgraded to source-only.
  - ALL 48, POST-fix (probe661_full_after.*): (14 pages, 0 errors,
    0 demoted). Every split row renders.
  - ALL 48, PRE-fix (probe661_full_before.*): (13 pages, 0 residual
    errors, 48 demoted). Every split row was demoted to "(not rendered)"
    before this fix, on this document, in a real compile — not a
    hypothetical.

So the measured effect on this document, stated the way 659 states it:
48 of 48 `split`-carrying equations move from silently demoted
("(not rendered)", mathematically correct content the reader never sees)
to correctly rendered, with 0 gate-refusal change and 0 residual compile
errors either side of the fix (the pre-fix "0 errors" was a demotion count
in disguise, not evidence of correctness).

NOT REBUILT: report.pdf/residuals.pdf/etc. for 1911.08029 were not rebuilt
— the corpus rebuild is on hold per the brief; every number above was
measured in memory / in throwaway probe cells under out/661/, never by
touching a published artefact.

FIX ROUND 1 — THE PUBLISHED PATH WAS NOT SWITCHED
======================================================
Blocking review findings 1-3. `docs/HANDOVER.md:25-42` — read for the
original task, apparently not acted on — names the published surface for
all 21 live documents explicitly: `evidence-*.pdf`/`residuals.pdf`, built
by `pdfdrill evidence`/`pdfdrill residuals` through `src/pdfdrill/
reports/`. `report.pdf`/`reporttex` (the only path fix round 0 touched)
"is retired from the site" / "superseded for reading, kept as the ink
chain's measure build." The reports package reimplements the exact
"does this row render" decision in two more places, neither switched, and
neither mentioned in the first pass:

  - `src/pdfdrill/reports/tex.py:30`, `_rendered()` — backs
    `render_row`/`render_table`, which back `evidence.py`'s `build()`
    (`pdfdrill evidence`, aliased `pdfdrill report`) and `residuals.py`
    (`pdfdrill residuals`, aliased `pdfdrill breport`). Called bare
    `rt.renderable(r.latex)`.
  - `src/pdfdrill/reports/residuals.py:55`, `findings()` — a SECOND,
    independent classification of "does this row render" (this module's
    own docstring: "Selection reuses report_tex.findings_rows" is aspirational
    for the SELECTION shape, not the render decision — the actual code
    reimplements the check over row objects rather than delegating to
    `findings_rows`, so fixing `findings_rows` in fix round 0 did not fix
    this). Called bare `rt.renderable(latex)`.

FIX: both switched to `rt.display_safe(...)`, one line each, matching the
seven sites already switched inside `report_tex.py`:

    # reports/tex.py, _rendered()
    safe = rt.display_safe(r.latex) if r.latex else ""

    # reports/residuals.py, findings()
    if not rt.display_safe(latex):

WHERE THE MAPPING BELONGS — the seam question, answered
-------------------------------------------------------------
`reports/from_document.py`'s own docstring states the package's design
rule directly: "The ONLY model-aware module in the package." `reports/
tex.py` and `reports/residuals.py` never touch the `Document`/DocGraph;
they operate on plain `EvidenceRow`/`EquationRow` dataclasses (`.latex`
is a bare `str`) and already import `report_tex` freely for exactly this
kind of string-level helper — `reports/tex.py`'s own docstring: "Imports
report_tex's cell helpers; it copies nothing, so a fix there is a fix
here." `esc_text`, `conf_cell`, `preamble`, `compile_fixpoint`,
`corrected_pairs`, `flagged_split`, `findings_tex` are ALL already called
this way from `reports/`. `to_inline_env`/`display_safe` are the same
kind of helper — a pure `str -> str` transform-then-gate with no model
awareness at all — so calling `rt.display_safe(...)` from `reports/tex.py`
and `reports/residuals.py` is not importing across the model-aware seam,
it is using the seam exactly as every other cell-level decision in this
package already does. The alternative considered and rejected: moving
`to_inline_env`/`display_safe` INTO `reports/` (or duplicating the map
there) would put the same logic in two places for one gate — the opposite
of "a fix there is a fix here" — and would still need `report_tex.py`'s
own `renderable()`/`_strip_innermost_envs`/`has_bare_align_marker`
machinery, so nothing is actually gained by relocating it. Applying the
map at ROW CONSTRUCTION time (inside `from_document.build_rows`, the one
model-aware module) was also considered and rejected: `EquationRow.latex`
is read for BOTH the LaTeX-source cell and the Rendered cell in
`render_row()` — mapping it at that point would corrupt the published-form
decision fix round 0 made (the source column must stay the untransformed
reading), exactly the trap the design-question section above already
worked through once for `report_tex.row()`.

RE-MEASURED, 1911.08029, through the PUBLISHED path (evidence.py + residuals.py)
-------------------------------------------------------------------------------------
Script: ~/pdfdrill-library/1911.08029/out/661/661_evidence_measure.py. Runs
`reports.from_document.build_rows()` (the real row objects, from the real
model — no tiddlers, no substitute), then `reports.evidence.EV.build()`
and `reports.residuals.RS.findings()`/`RS.select()`/`RS.build()` — the
SAME functions `cmd_evidence`/`cmd_residuals` call — writing into
`out/661/throwaway_before/` and `out/661/throwaway_after/` (never the
document's own folder: 1911.08029 carries no published `evidence-*.pdf`/
`residuals.pdf` at all today, only the old `report.pdf`, whose mtime
predates this session and is untouched). "before" reverts ONLY
`rt.display_safe` to bare `rt.renderable` (the exact pre-fix-round-1
state); everything else, including `renderable()`/`to_inline_env()`
themselves, is untouched.

evidence-equation.pdf (the artefact `evidence.py` actually writes, the one
`docs/HANDOVER.md` names published):
  BEFORE: 11 pages, 0 (residual) errors, 48 demoted. Verified directly by
    grepping the built .tex: exactly 48 occurrences of the literal string
    "(not rendered)" — one per split-carrying equation, none elsewhere
    (consistent with fix round 0's gate-refusal count of 0/94: nothing
    else on this document was ever refused).
  AFTER: 12 pages, 0 errors, 0 demoted. 0 occurrences of "(not rendered)".

So the exact defect named in blocking finding 1 — a split-carrying
equation silently demoted on the artifact that ships — is confirmed
present before this round's fix and confirmed gone after it, on the
PUBLISHED path this time, not the superseded one.

residuals.pdf / `findings()`:
  BEFORE and AFTER both: `findings()` returns 0 unresolved rows either
  way, and `RS.build()` writes "Nothing open" both times (1 page, 0
  errors, 0 demoted, unchanged). This is NOT evidence the finding-2 fix
  has no effect — it means THIS document has no `report.ink.json` (no
  ink measurement has ever been run for it — confirmed, no `*.ink.json`
  file exists anywhere under its folder), and residuals.pdf on an
  unmeasured document draws every equation/formula band from ink/
  confidence classification that a `split`-carrying row never enters
  either way, matching fix round 0's own finding that raw `split` is
  never gate-refused (`renderable(raw_split)` returns the input
  unchanged) — so `findings()`'s bare-gate call was never actually
  MIS-classifying anything on THIS document's specific population. The
  `residuals.py:55` fix is still necessary and correct — it is a genuinely
  independent classification site that could disagree with `report_tex.
  findings_rows` on some OTHER malformed input, and the review named it
  as its own defect regardless of measured impact here — but the honest
  report is: 0 measured delta on residuals.pdf for THIS document.

CORRECTION (fix round 2). The sentence above originally said this was
"verified by the unit test (`test_findings_does_not_flag_a_display_
environment_as_unresolved`)" — false in the way that matters: that test
used a `split` fixture, and `findings()`'s call
(`if not rt.display_safe(latex):`) reads only the BOOLEAN, never the
returned string. Round 0's own measurement already showed the bare gate
never refuses split (or any of the other six MAP names) either way, so
the boolean this line branches on is IDENTICAL whether `display_safe` or
bare `renderable` is called, for that population — the test passed
before this fix existed too. Proven directly (not asserted): reverting
`rt.display_safe` to bare `rt.renderable` and re-running the exact same
`findings()` call with the exact same fixture gives the exact same
result, `unresolved == []`, either way.

So: does the `residuals.py:63` switch do anything at all? Checked what
`findings()` actually uses (per the instruction that decides this) — the
BOOLEAN only, never the returned string — so a discriminating input
needs the BOOLEAN itself to differ, not the mapped value. One exists, and
it is not from the seven-pair MAP: `to_inline_env`'s `equation`/
`equation*` STRIP is a different mechanism (deletion, not renaming), and
it can EXPOSE a bare marker the wrapper was shielding.
`renderable(r"\begin{equation} a & b \end{equation}")` is truthy (the
generic env-strip in `_strip_innermost_envs` shields ANY environment's
own markers, `equation` included, so the wrapped `&` never reaches
`has_bare_align_marker`); `display_safe()` on the same value is falsy
(the map deletes the wrapper FIRST, so the same `&` is now bare at depth
0 when the gate runs). Verified in `findings()` itself, not just in
isolation: with `rt.display_safe` unmodified this row IS added to
`unresolved`; with `rt.display_safe` reverted to bare `rt.renderable` it
is NOT — a genuine, reproducible discriminating case for this exact call
site's boolean.

The misleading test was renamed and rewritten, not just re-argued in
prose: `test_findings_does_not_flag_a_display_environment_as_unresolved`
-> `test_findings_uses_display_safe_verified_by_reverting_it`
(tests/test_reports_residuals.py). It keeps the `split` assertion as a
documented FACT (still true, just explicitly labelled non-discriminating,
with no revert-check attached to it) and adds the `equation`-strip case
as the actual regression guard, verified inside the test by reverting
`rt.display_safe` via `monkeypatch` and confirming the assertion THEN
fails to hold (the row is no longer unresolved) — the same method the
reviewer used.

THE SAME GAP, FOUND A SECOND TIME, PROACTIVELY. Checked whether fix
round 0's own analogous test had the identical structural problem, since
`unresolved_formulas()` uses the identical shape
(`if latex and not display_safe(latex):`, boolean only): it did.
`test_unresolved_formulas_agrees_with_the_actual_rendered_cell`
(tests/test_report_tex.py) used the same `align` fixture and passed
identically under the same revert, for the same reason, undetected by
the first review because it targeted the `reports/` sites specifically.
Renamed and rewritten the same way, without being asked, because leaving
a second known instance of the exact defect just flagged is the same
evidence-discipline failure in smaller form: `test_unresolved_formulas_
agrees_with_the_actual_rendered_cell` -> `test_unresolved_formulas_
uses_display_safe_verified_by_reverting_it`. Same shape: the `align`
assertion kept as a documented non-discriminating fact, the `equation`-
strip case added as the real guard, verified by reverting.

Both renamed tests were independently confirmed to discriminate by
reverting `report_tex.display_safe` to bare `renderable` at the MODULE
level (not just inside their own `monkeypatch`) and re-running the full
targeted set: both, and only the tests that were already known to
discriminate (`test_display_safe_maps_before_gating_for_every_pair`,
`test_row_source_column_keeps_the_original_render_uses_the_mapped_form`,
`test_rendered_maps_a_display_environment_but_source_keeps_it_raw`),
FAIL; every other test in the targeted set still passes.

A document WITH an ink measurement and a split-carrying flagged/doubted/
low-confidence row would be needed to show a residuals.pdf-LEVEL (not
unit-level) before/after delta for the `equation`-strip case or any
other; none was found in the corpus search above (the 9 split-hit
documents were not cross-checked against `report.ink.json` presence —
left open below). This remains a real, disclosed gap: the boolean-level
fix is now genuinely tested, but no PUBLISHED-artefact-level number
demonstrates it, because the discriminating input (`equation`+bare
marker) was not found to occur in this corpus's Equation.latex the way
`split` was — it was constructed to prove the switch is not inert, not
measured from a real document.

NOT REBUILT (fix round 1): no published artefact was rebuilt here either
— `~/pdfdrill-library` git status stays clean and 1911.08029/report.pdf's
mtime is untouched; the evidence/residuals builds above ran entirely into
`out/661/throwaway_before/`/`throwaway_after/`, both under the
deny-by-default `/*` library `.gitignore`.

TESTS
-----
tests/test_report_tex.py, 10 new tests (73 in the file, was 63):
  - test_to_inline_env_maps_each_of_the_seven_pairs (Rule 17: every fixture
    is a genuine display environment)
  - test_to_inline_env_maps_starred_forms_too
  - test_to_inline_env_strips_equation_wholesale
  - test_to_inline_env_maps_not_unwraps (trap 1, both checks)
  - test_split_alone_reaches_renderable_but_is_not_yet_typesettable (states
    the auditor's own framing as a fact, before testing the fix)
  - test_display_safe_maps_before_gating_for_every_pair
  - test_renderable_itself_does_not_map (the design decision, checked
    directly against the gate)
  - test_row_source_column_keeps_the_original_render_uses_the_mapped_form
    (the published-form decision, checked against real `row()` output)
  - test_unresolved_formulas_uses_display_safe_verified_by_reverting_it
    (RENAMED, fix round 2 — see below; was
    test_unresolved_formulas_agrees_with_the_actual_rendered_cell)
  - test_split_alone_no_longer_won_t_work_here (real xelatex compile,
    gated on `shutil.which("xelatex")`; reproduces the exact named error
    before, and its absence after)

FIX ROUND 1 — two more, against the published-path call sites:
tests/test_reports_tex.py, 1 new test (7 in the file, was 6):
  - test_rendered_maps_a_display_environment_but_source_keeps_it_raw
    (`_rendered()`/`render_row()` — the site backing evidence-equation.pdf/
    report.pdf; Rule 17 fixture; checks the Rendered cell is mapped AND
    the LaTeX-source cell stays raw, in one real `render_row()` call)
tests/test_reports_residuals.py, 1 new test (20 in the file, was 19):
  - test_findings_uses_display_safe_verified_by_reverting_it (RENAMED,
    fix round 2 — see below; was
    test_findings_does_not_flag_a_display_environment_as_unresolved)

FIX ROUND 2 — two RENAMED, not added (counts above already reflect this;
no test file count changes, only test bodies/names): the review found
`test_findings_does_not_flag_a_display_environment_as_unresolved` passes
identically whether `reports/residuals.py:63` calls `display_safe` or the
bare gate — proven by reverting and re-running. Checked what `findings()`
uses (only the boolean, never the returned value): a genuinely
discriminating input still exists for the boolean, from `to_inline_env`'s
`equation`/`equation*` STRIP rather than the seven-pair MAP
(`\begin{equation} a & b \end{equation}` passes the bare gate, is refused
after the map). Rewrote the test in place — same slot, honest new name —
to keep the ORIGINAL `split` assertion as a documented non-discriminating
FACT and add the `equation`-strip case as the actual regression guard,
verified inside the test by reverting `display_safe` via `monkeypatch`
and confirming the assertion then fails to hold. Checked report_tex.py's
own analogous test (`unresolved_formulas()` has the identical
boolean-only shape) proactively and found the SAME defect there,
undiscovered by the first review because it targeted `reports/`
specifically — renamed and rewritten the same way, unasked, rather than
left as a second known instance of the same gap. See the "CORRECTION
(fix round 2)" note in the MEASURED/residuals section above for the full
argument and the direct verification (reverting `display_safe` at module
level and confirming exactly these two tests, plus the three already-
known-discriminating ones, are the only ones in the targeted set that
fail).

tests/test_alphabet_safe.py: one PRE-EXISTING test
(`test_the_source_column_is_not_what_this_touches`) asserted `row()`'s
source verbatim, via `inspect.getsource`, for the literal string
`"renderable(latex)"` — broken by this task's intentional change (the
Rendered cell now reads `display_safe(latex)`). Updated to check for
`"display_safe(latex)"` and, separately, that `renderable()`'s own source
still runs `alphabet_safe` (the property that test actually cares about),
rather than silently accepting the stale assertion.

Full suite: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest
tests/ -q -p no:cacheprovider`
  base commit f25ca1cc:      3549 passed
  after fix round 0:         3559 passed (10 new tests; 0 regressions)
  after fix round 1:         3561 passed (2 more new tests; 0 regressions)
  after fix round 2 (this):  3561 passed (2 tests RENAMED/rewritten in
                             place, 0 added/removed, 0 regressions)

EVIDENCE
--------
Both required, both produced (updated for fix round 1):
  ~/pdfdrill-library/out/661/INSPECT.txt (corpus-level, no single document —
    525's convention) — 3 paths, unchanged by fix round 1: the
    grep-candidate list, the DocGraph re-check script, and its output
    naming 1911.08029.
  ~/pdfdrill-library/1911.08029/out/661/INSPECT.txt — 36 paths (was 23):
    the original 23 (gate/single-cell measurement + the superseded-path
    `row()`/`compile_fixpoint` probes, now labelled FIX-ROUND-0/superseded
    in the file's own reasons) plus 13 new for fix round 1 — the
    published-path measurement script and, for both `throwaway_before/`
    and `throwaway_after/`, `evidence-equation.tex/.log/.pdf` and
    `residuals.tex/.log/.pdf`. All 36 checked to exist and be non-empty by
    `pdfdrill.taskout.inspect_list` itself (0 failed).

WHAT IS OPEN
------------
  - align/alignat/flalign/eqnarray/gather/multline have zero measured
    occurrences in any sampled document's Equation.latex today — the fix
    is untested against a REAL corpus row for six of the seven pairs
    (unit-tested directly, but not corpus-measured, per the search above).
    If a future document's OCR/extraction path ever emits one of these six
    raw, this fix already covers it; nothing further is needed, but it has
    not yet been PROVEN necessary beyond `split`.
  - `region_render()` and `listing_cell()` were deliberately left unmapped
    (see the design-question section) — if a corpus document is ever found
    with a display environment inside an image-region LaTeX value or an
    lstlisting's inline math span, that is a new, separate finding, not a
    regression of this task.
  - No published artefact was rebuilt (report.pdf/residuals.pdf/etc. for
    1911.08029, or anything for 0902.0431), per the brief. The corpus
    rebuild is on hold until inkdrill's in-line formula marking lands.
  - FIX ROUND 1/2: `residuals.py::findings()`'s fix has 0 measured delta on
    1911.08029 specifically (no ink measurement exists for it, so no
    equation ever enters a flagged/doubted/lowconf band there regardless
    of this fix) AND 0 measured delta from the `split` population
    corpus-wide (fix round 2: the boolean this call site branches on is
    structurally identical to the bare gate for all seven MAP names,
    proven by reverting). The only DISCRIMINATING case found for this call
    site's boolean (the `equation`/`equation*` STRIP) is unit-tested, not
    corpus-measured — no document in the 9-hit corpus search was checked
    for an `equation`-wrapped bare-marker equation, and none was expected
    to have one (it is a constructed, plausibly-rare malformed-OCR shape,
    not something observed in this library). Finding a document that both
    HAS an ink measurement (`report.ink.json`) AND carries either a
    split-hit or an equation-strip-discriminating identifier, to show an
    actual residuals.pdf-level before/after delta for either mechanism,
    was not done — the 9 split-hit documents were not cross-checked
    against ink-measurement presence, and no corpus search was run for the
    equation-strip case at all.
  - `tools/census509.py`, `tools/midstring486.py`, `tools/bed492.py`,
    `tools/refine444.py`, `tools/beddiag492.py`, `tools/rightdelim517.py`,
    `tools/prompt448.py` (review finding 6, informational, not blocking)
    call `rt.renderable()` directly and were not switched — one-off
    diagnostic scripts outside the command surface (`commands.yaml`), not
    shipped artefacts. Left untouched; named here so a future pass does
    not have to rediscover them.
