# 010 — Reference stub at first citation

## Status: implemented, tested, and the penev_A target met after three fix rounds.

Round 0 put stub-creation solely in `CitationProcessor.process_objects`, a
model-build-time post-pass — correct in isolation, but penev_A showed most of
its Citations are created LATER, by `pdfdrill/bibliography.py`, so only 1 of
52 keys got a stub. Reported as NEEDS_CONTEXT rather than guessed at. The
coordinator's ruling (fix round 1): the spec's own words — "Create an empty
Reference on first Citation" — apply wherever a Citation is created, so
stub-creation moved into ONE helper called by every Citation creator.
penev_A then measured 52 References / 52 distinct keys, all stubs — but 162
`cites` Alignments for 81 Citations, because the same edge got added twice
(once by the new helper at creation time, once by `cmd_bibliography`'s
unconditional relink pass). Fix round 2: made edge-creation idempotent
through one shared helper; penev_A then measured exactly 81 `cites`
Alignments for 81 Citations — but re-running `cmd_bibliography --force` a
SECOND time duplicated Citations (81 → 106), because its cleanup step
decided "did bibliography already run" by whether a non-stub Reference
existed, and 010's own stubs made that always-false once a document had
gone through this fix. Fix round 3 (this round): the cleanup gate now
decides by provenance (`added_by`) instead, regardless of whether every
Reference is a stub; penev_A now measures 81/52/52/81, twice in a row.

## WHAT CHANGED

- `src/docmodel/modules/citation.py` — new module-level
  `ensure_reference_stub(doc, citation: DocObject, bibkey: str) -> Optional[DocObject]`.
  Returns the existing Reference for `citation`'s citekey (a stub from an
  earlier citation with the same key, or one already filled by
  bibliography/bibsource/bibfetch) if there is one; otherwise creates a new
  stub (`props`: `citekey`, `bibkey`, `stub: True`, `ref_source: "citation"`)
  anchored at THIS citation's own surface Realization (same stream, same
  start/end anchor — reused, not a new one). Either way, links `citation` to
  the Reference via a `cites` Alignment (`props`: `citekey`, and `number`
  when the citation carries one — the same edge shape
  `bibliography.link_citations` produces, needed so an existing test
  asserting every `cites` edge carries a `number` didn't regress). No-ops
  (returns `None`) when the citation has no citekey or no surface
  Realization. Lives in `docmodel` (not `pdfdrill`) so `pdfdrill/
  bibliography.py` can import it with no cycle.

  Four call sites now call it, each right after `doc.add(obj)` /
  building the Citation, so "first occurrence" falls out of creation order
  with no separate pass and no population any one caller can't see:
  1. `CitationProcessor.create_object` (`src/docmodel/modules/citation.py`,
     `[key]`-bracket citations) — bumps `reference_stubs_created` only when
     no Reference existed for the key a moment before (counter semantics
     unchanged from round 0).
  2. `detect_numeric_citations` (`src/pdfdrill/bibliography.py` ~line 271,
     `[N]`/`[N,M]`/`[N-M]`).
  3. `detect_author_year_citations` (~line 334, `(Author, Year)` over
     `mathpix_lines`).
  4. `detect_author_year_in_objects` (~line 401, the stream-agnostic
     `[Author, Year]`/`(Author, Year)` scan over prose objects, for
     markdown/source models with no `mathpix_lines`).

  Each of the three `bibliography.py` sites pulls `bibkey` from
  `doc.meta.get("bibkey") or ""` (they're plain functions, not module
  instances with `self.bibkey`) and imports `ensure_reference_stub` locally,
  matching the file's existing per-function local-import style.

- `src/pdfdrill/bibliography.py` (unchanged from round 0, still current):
  - New `drop_dangling_cites(doc)` — removes `cites` Alignments whose right
    side no longer resolves to a Reference in the document; one that still
    resolves (typically a stub) survives. Used everywhere the old code
    blanket-cleared `cites` edges, so a stub's link survives a `--force`
    rebuild or a gold ingest instead of being destroyed and only sometimes
    relinked.
  - `add_reference_objects` (parsed References section, `cmd_bibliography`) —
    looks up a stub by citekey first; if found, fills its props in place
    (drops `stub`, sets `ref_source: "text"`) and keeps its id/anchor;
    otherwise creates as before.
  - `ingest_bbl` (author's `.bbl`, `cmd_bibsource`) — same fill-in-place
    pattern; `ref_source` set to the `source` arg (`"bbl"`/`"bibitem"`).
  - `load_bibtex_file` (`.bib`, `cmd_bibsource` and the `restrict`-ed path,
    ~line 486) — already merged fields onto any pre-existing Reference by
    key; added the one-line stub-clearing step (`pop("stub")`,
    `ref_source: "bib"`) when the matched Reference was a stub.
  - `build_bibliography_from_source` docstring: "no References exist yet" →
    "no *non-stub* References exist yet".

- `src/pdfdrill/commands.py` (unchanged from round 0):
  - `cmd_bibliography` — the "already ran" gate excludes stubs; the
    `--force` drop path only drops non-stub References and calls
    `drop_dangling_cites`.
  - `cmd_bibsource` — the "author's bibliography is authoritative, drop
    prior References" step keeps stubs and uses `drop_dangling_cites`.
  - `cmd_bibfetch` (API and keyless-delegate paths) — pops `stub` when an
    enrichment lands, so a bibfetch-filled stub reads as filled.

- `tests/test_reference_stub.py` — 5 tests (was 4):
  - `_run_citation_processor` simplified to one `process_document(doc)` call
    (stub creation is now inline in `create_object`; there is no
    `process_objects` to also call).
  - The 4 round-0 tests unchanged in intent: 3-line fixture (`[a]`, `[b]`,
    `[a, c]`) → 3 stubs / 4 Citations, anchor-at-first-occurrence, every
    Citation linked via a `cites` Alignment (disambiguated by
    `props.citekey` since two citations from a comma list share one line
    anchor), and `add_reference_objects` filling a stub in place while
    leaving the other two untouched.
  - New: `test_bibsource_bib_fill_updates_stub_in_place` — calls
    `bibliography.load_bibtex_file` (the ~line-486 `.bib` creator
    `cmd_bibsource` uses) with a synthetic `.bib` entry for `"a"`; asserts
    `created == 0` (filled, didn't duplicate), the stub's id and anchor are
    kept, `stub` is dropped, `ref_source` becomes `"bib"`, and `b`/`c` are
    untouched. Pinned per the coordinator's ruling because the corpus
    measurement below can't exercise this path (no `.bbl`/`.bib` on disk for
    penev_A).

- `src/docmodel/modules/citation.py` (fix round 2) — new module-level
  `add_cites_alignment(doc, left: Range, right: Range, props: dict) -> Optional[Alignment]`,
  the one place a `cites` Alignment gets added. Skips (returns `None`) when
  an identical one — same `left`, same `right`, **and same `props["citekey"]`**
  — already exists; otherwise creates it and returns it.
  `ensure_reference_stub` now calls this instead of `doc.add_alignment`
  directly, and so does `bibliography.link_citations` (imported locally,
  same style as the other three call sites). `link_citations`'s docstring
  now says it's idempotent and that its return value is edges ADDED, not
  the total linked count.

  `citekey` had to be part of the identity check, not just `left`+`right`:
  a citation is anchored at LINE granularity (`start == end == the line`,
  sub-position is an `offset`/`length` prop, not part of the Range), so two
  DIFFERENT, never-seen-before citekeys parsed off the SAME line (e.g.
  `(Oja, 1989; Sanger, 1989; Plumbey, 1991)`) share one `left` Range — and
  each gets its own brand-new stub anchored at THAT line, so their `right`
  Ranges collide too. A first cut that deduped on `(left, right)` alone
  silently dropped one of every such pair as "already linked" when it was a
  genuinely distinct edge to a genuinely distinct Reference — caught by the
  penev_A measurement (71 edges for 81 Citations, 10 short) before it was
  caught by a test; `citekey` in the identity tuple fixed both.

- `tests/test_bibliography.py::test_numeric_citation_detection_and_linking`
  and `test_author_year_citation_detection_and_linking`, and
  `tests/test_bibsource.py::test_detect_author_year_in_objects_square_brackets_and_folding`
  — updated. All three pre-dated 010 and asserted `link_citations(doc) == N`
  meaning "N NEW edges added"; now that `detect_*` already links via
  `ensure_reference_stub` before `link_citations` ever runs, the correct,
  idempotent answer is 0 new edges (already linked), not N. The
  `test_bibsource.py` one also surfaced a genuine, separate, unfixed gap
  (see OPEN): `ensure_reference_stub`'s exact-citekey match can pre-empt
  `link_citations`'s fuzzy surname+year prefix match, so an abbreviated
  citekey (`"carlsson2009"`) that would have fuzzy-matched a fuller gold key
  (`"carlsson2009topology"`) instead gets its own spare stub Reference; the
  test now asserts that actual (imperfect) shape rather than silently
  weakening to match it.

- `tests/test_reference_stub.py` (fix round 2) — 2 new tests (7 total):
  - `test_link_citations_is_idempotent_against_ensure_reference_stub` (the
    one the ruling asked for) — runs the fixture through `CitationProcessor`
    (which calls `ensure_reference_stub` per Citation) then
    `bibliography.link_citations`; asserts 4 `cites` Alignments both before
    and after (one per Citation), and that `link_citations` returns 0 newly
    added.
  - `test_two_new_keys_sharing_one_line_each_get_their_own_edge` — a
    regression test for the citekey-collision bug found while measuring
    penev_A (above): two brand-new citekeys on one line (`[d, e]`) each get
    their own edge, not one dropped as a false duplicate of the other.
    Verified it actually catches the bug: reverting `add_cites_alignment`'s
    identity check to `(left, right)` only (dropping `citekey`) fails this
    test with `0 == 1`; restoring the fix makes it pass again.

- `src/pdfdrill/commands.py` (fix round 3) — `cmd_bibliography`'s `--force`
  cleanup step now runs unconditionally on `--force` (no longer gated on
  `existing` — non-stub References — being non-empty) and retracts by
  provenance: every `Citation` or `Reference` with `props["added_by"] ==
  "bibliography"`, then `drop_dangling_cites`. The old gate decided "did
  this command already run" by "does a non-stub Reference exist", which
  010's own stubs made permanently false for a document whose References
  are all still stubs — a state true right after every `model` build, so
  the cleanup was skipped and a second `--force` piled a fresh detection
  pass on top of the untouched first one (81 → 106 Citations, measured).
  The `if existing and not force: return` early-return gate (a SEPARATE
  check, "already ran without --force, use the cache") is UNCHANGED — the
  ruling scoped this fix to `--force` idempotency specifically; the
  early-return gate has the same shape of bug for a non-force rerun and is
  flagged, not fixed, in OPEN.

- `src/docmodel/modules/citation.py` (fix round 3) — `ensure_reference_stub`
  now stamps a new stub's `added_by` from the CREATING CITATION:
  `citation.props.get("added_by") or "citation"`. A stub made for a
  `detect_*`-created citation (`added_by: "bibliography"`) inherits that
  marker, so the new cleanup above retracts the Citation and its stub
  TOGETHER. A stub made for a `CitationProcessor`-created citation (which
  carries no `added_by` of its own) gets the literal marker `"citation"` —
  never matches `"bibliography"`, so it and its Citation both survive a
  `cmd_bibliography --force`, filled or not.

- `src/pdfdrill/bibliography.py` (fix round 3) — `add_reference_objects`'s
  FRESH-creation branch (no matching stub) now also stamps `added_by:
  "bibliography"` — this command made that Reference from nothing, so a
  `--force` rerun should retract and re-derive it exactly like a
  `detect_*`-created Citation. The FILL branch (an existing stub found)
  deliberately does NOT touch `added_by` — a `CitationProcessor` stub this
  fills must keep reading "citation", not start reading "bibliography", or
  it would get wrongly retracted (and its Citation left without a
  Reference) on the very next `--force`.

- `tests/test_reference_stub.py` (fix round 3) —
  `test_cmd_bibliography_force_is_idempotent_on_an_all_stub_document`: builds
  a Document (via `model_io.save_model` + a throwaway PDF, the same harness
  `test_bibsource.py::test_cmd_bibliography_falls_back_to_source_bib`
  already uses) with a REPEATED author-year citation on a separate line
  (`(Asai, 2023)` on two different lines, `(Wu, 2024)` on a third), calls
  `commands.cmd_bibliography(pdf, force=True)` twice, and asserts the
  (Citation, Reference, `cites`-Alignment) triple is identical after both
  calls. The repeat-on-a-separate-line shape matters: a fixture with only
  distinct, single-occurrence citekeys would pass even with the bug still
  present, because `ensure_reference_stub` anchors each stub at its OWN
  citation's line, so `cmd_bibliography`'s `ref_anchors` exclusion (skip
  re-scanning a Reference's own line) already happens to exclude every
  FIRST-occurrence line on a rerun regardless of whether cleanup is fixed —
  only a SECOND occurrence of an already-stubbed key, sitting on a
  different, non-excluded line, is free to re-detect and duplicate.
  Verified it actually catches the bug: reverting the `commands.py` gate to
  the old `if force and existing:` form fails this test with `(4, 2, 3) !=
  (3, 2, 3)`; restoring the fix makes it pass again.

No prop-contract change was needed: `stub` isn't in the committed
`corpus_props.json` snapshot (a walk of the library, last done for task 290,
not re-run here) so `table_violations()` doesn't see it yet; `ref_source:
"citation"` is a new value of an already-documented prop, not a new prop;
`added_by` is an existing, widely-used generic provenance prop (Equation,
Section, ...) and `"bibliography"` was already one of its values (on
Citations) before this round.

## THE MEASUREMENT — four rounds

Backup first, per the brief: `~/pdfdrill-library/penev_A/out/010/
model.before.json` (+ `model.before.docpack.json`), copied before touching
anything.

`pdfdrill status penev_A.pdf` and the sidecar (`penev_A.drill.json`) were
checked for what supplied the "8 References" the brief's spec describes: no
`BIBSOURCE_BUILT` fact, no `bibsource_*` evidence keys, and no `.bbl`/`.bib`
file anywhere under `~/pdfdrill-library/penev_A/` or in the extracted
`texzip/` (a MathPix-sourced scan, not an arXiv LaTeX source — no `texsrc/`
either). **The inputs that supplied the 8 are not on disk — per the brief,
this half of the measurement stops there** (unchanged by either fix round;
see OPEN).

**Round 1** (`CitationProcessor.process_objects` only): `model --force`
alone found 1 Citation / 1 key (`[dB]`, a false-positive units abbreviation —
this paper cites `(Author, Year)`-style). 80 of the pre-existing model's 81
Citations carried `added_by: "bibliography"` — created later by
`pdfdrill bibliography`'s author-year detectors, a population
`CitationProcessor`'s post-pass never saw. After `model --force` +
`bibliography --force` (restoring those 80): 81 Citations / 52 keys but
still only **1** Reference.

**Round 2** (fix round 1 — `ensure_reference_stub` called by all four
Citation creators): the SAME sequence now gives all 52 References, but 162
`cites` Alignments — 2 per Citation, not 1.

**Round 3** (fix round 2 — idempotent `add_cites_alignment`, this round):
exactly 81 `cites` Alignments, one per Citation. Measured with
`out/010/round3_script.py`, NOT a plain rerun of `bibliography --force` on
the round-2 model: doing that literally was tried first and found a THIRD,
separate defect (see OPEN) — with every Reference still a stub,
`cmd_bibliography`'s "already ran" gate (added in fix round 1) reads an
all-stub document as "never ran", so `--force`'s own cleanup step (drop
previously-detected Citations before re-detecting) doesn't run either, and
Citations went 81 → 106 (duplicated) instead of staying at 81. `round3_script.py`
avoids this by restoring the clean `model --force`-only baseline
(`model.after_model_force_v2.json`, 1 Citation/1 stub/1 edge — unaffected by
either fix round) and running `bibliography --force` against THAT, once,
cleanly.

| state | Citations | distinct citekeys | References | stubs | `cites` edges |
|---|---:|---:|---:|---:|---:|
| before any rebuild (backup) | 81 | 52 | 0 | — | — |
| round 1: after `model --force` alone | 1 | 1 | 1 | 1 | 1 |
| round 1: after + `bibliography --force` | 81 | 52 | **1** | 1 | 1 |
| round 2: after `model --force` alone | 1 | 1 | 1 | 1 | 1 |
| round 2: after + `bibliography --force` | 81 | 52 | 52 | 52 | **162** |
| round 3: after `model --force` alone | 1 | 1 | 1 | 1 | 1 |
| round 3: after + `bibliography --force` | 81 | 52 | 52 | 52 | 81 |
| round 4: after `model --force` alone | 1 | 1 | 1 | 1 | 1 |
| round 4: after + `bibliography --force` (run 1) | **81** | **52** | **52** | **52** | **81** |
| round 4: after + `bibliography --force` (run 2) | **81** | **52** | **52** | **52** | **81** |

**Round 4** (fix round 3 — the `--force` cleanup gate decides by
`added_by`): `bibliography --force` run twice in a row, starting from the
`model --force`-only baseline, lands on 81/52/52/81 BOTH times — the
ruling's expectation, met.

The ruling also asked to restore "the round-2 snapshot" specifically and run
`--force` twice from there. Doing that literally turned up a real,
explicable wrinkle instead of 81/52/52/81 both times, recorded rather than
fudged: `model.after_bibliography_v2.json` (round 2's own output) predates
this round's `added_by` marker — none of its 52 stub References carry one,
because round 3 is the change that starts stamping it. The new cleanup only
retracts a Reference whose `added_by == "bibliography"`; these 52 have none,
so they survive even though the 80 Citations they belonged to DO get
retracted (Citations already carried `added_by` before this round). The 52
orphaned stubs then make `ref_anchors` (the "don't re-scan a Reference's own
line" exclusion set `cmd_bibliography` builds before re-detecting) exclude
most of the document's citation-bearing lines, so re-detection only turns up
the citations on the handful of lines nothing was ever anchored at. Result:
a STABLE 26 Citations / 17 keys / 52 References / 162 edges, both times —
idempotent, but not 81/52/52/81, because that snapshot is pre-migration
data, not because the fix doesn't hold going forward. Both scenarios are in
`run4_script.py` / `run4_result.json` side by side: Scenario A is the
literal ask (round-2 snapshot, stable at 26/17/52/162); Scenario B is the
`model --force`-only baseline (stable at 81/52/52/81, matching "expect
81/52/52/81 both times"). Flagged in OPEN as a one-time upgrade caveat for
any document that had `bibliography` run on it before this fix landed.

Scripts + all four rounds' numbers: `~/pdfdrill-library/penev_A/out/010/
script.py` → `result.json` (keys `round1_process_objects_only_covers_
CitationProcessors_own_keys`, `round2_..._but_not_idempotent`,
`round3_add_cites_alignment_shared_and_idempotent`, and
`round4_force_cleanup_gate_decides_by_added_by`; re-running reproduces all
four from the saved model snapshots in `out/010/`), `out/010/round3_script.py`
→ `round3_result.json` (round 3's before/after), and `out/010/run4_script.py`
→ `run4_result.json` (round 4's two scenarios) — all independently
re-runnable against the live PDF via the actual CLI.

## WHAT WAS LOST by `model --force`, and how to restore

Unchanged from round 0's finding: object-type counts are identical before vs.
immediately after `model --force` for every structural type (Page 22,
Footnote 53, Table 1, TableRow 15, TableCell 54, Sidenote 46, Toc 1, Abstract
1, Diagram 14, Section 37, Picture 11, ListItem 4, Equation 116, Formula 674,
Paragraph 222) — only Citation and Reference move, because `pdfdrill
bibliography`'s Citations/References aren't retracted when `BIBLIOGRAPHY_
BUILT` is dropped (HANDOVER-RULES §3, "Retraction task" — known, explicitly
out of scope here). **Restore**: `pdfdrill bibliography penev_A.pdf --force`
(done, all four rounds) regenerates the 80 author-year Citations; with the
fix rounds it also regenerates all 52 stubs, links every Citation exactly
once, AND — as of this round — stays at that state under a repeated
`--force`. The library document was left in the round-4 restored state (81
Citations/52 keys/52 stub References/81 `cites` edges, confirmed stable
across two consecutive `--force` runs), not reverted to the pre-task
backup, consistent with treating `~/pdfdrill-library` as regenerable;
backups remain at `out/010/`.

## TESTS

    PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest tests/test_reference_stub.py -q -p no:cacheprovider
    -> 8 passed (round 0's 4 confirmed failing first, per the earlier report;
       the 4 added since -- bibsource-fill, idempotent-relink, the
       two-new-keys-one-line collision, and the cmd_bibliography --force
       idempotency test -- each written and run once before moving on, and
       each verified to actually fail without its corresponding fix)

    PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest tests/ -q -p no:cacheprovider
    -> fix round 1: first pass after wiring the 3 bibliography.py call sites:
       3095 passed, 1 FAILED (test_numeric_citation_detection_and_linking --
       asserted every `cites` edge carries `number`; ensure_reference_stub's
       only carried `citekey`). Fixed (add `number` when present). Rerun:
       3096 passed.
    -> fix round 2: after making add_cites_alignment idempotent, 1 FAILED
       (test_author_year_citation_detection_and_linking -- asserted
       link_citations(doc) == 2, stale now that detect_author_year_citations
       already links via ensure_reference_stub). Updated to assert 0 newly
       added / 2 total. Rerun: another FAILED
       (test_detect_author_year_in_objects_square_brackets_and_folding --
       same stale assumption, PLUS it exposed the citekey-vs-fuzzy-match gap
       in OPEN). Updated to assert the actual current shape (a spare stub
       for the unmatched abbreviated key) rather than silently weakening the
       count. Rerun: 3097 passed. Then, running the penev_A measurement,
       found the collision bug (71 edges for 81 Citations, see WHAT CHANGED);
       fixed `add_cites_alignment`'s identity check and added
       `test_two_new_keys_sharing_one_line_each_get_their_own_edge`. Final
       full-suite run: 3098 passed, 0 failed.
    -> fix round 3: after the `added_by`-provenance cleanup gate and the new
       idempotency test, no regressions: 3099 passed, 0 failed on first try
       (the new test's fixture was iterated on locally -- a first,
       single-occurrence-per-key version passed even with the bug
       deliberately reintroduced via `git stash`, so it was rebuilt with a
       repeat citation on a separate line before being counted as done; see
       WHAT CHANGED for why that shape is the one that actually exercises
       the bug).

## OPEN

- **The "8 filled by bibsource" half of the spec cannot be measured** for
  penev_A: no `.bbl`/`.bib` file exists on disk for this document, and the
  earlier 8 References this document apparently had were lost to the
  retraction gap before this task started (no `BIBSOURCE_BUILT` fact or
  `bibsource_*` evidence survives either, so even reconstructing what
  produced them isn't possible from this sidecar). The fill-in-place
  behavior itself IS pinned by unit test even though the corpus measurement
  can't exercise it: `tests/test_reference_stub.py::
  test_bibsource_bib_fill_updates_stub_in_place` calls `load_bibtex_file`
  (the `.bib` creator `cmd_bibsource` uses) directly against a stub and
  asserts it fills rather than duplicates.
- ~~`cites` Alignments accumulate duplicates...~~ **Fixed this round** (see
  WHAT CHANGED — `add_cites_alignment`, shared by `ensure_reference_stub` and
  `link_citations`). penev_A now measures 81 edges for 81 Citations.
- ~~`cmd_bibliography --force`'s cleanup step only runs when `existing` --
  non-stub References -- is non-empty...~~ **Fixed this round** (see WHAT
  CHANGED — the cleanup gate now decides by `added_by` provenance,
  unconditionally on `--force`). penev_A now measures 81/52/52/81 both
  after a first and a second `--force` in a row.
- **New, separate finding from measuring THIS fix (not a defect in the fix
  itself — a one-time upgrade caveat, not fixed, not asked for):** the new
  cleanup can't retroactively clean up a Reference created by
  PRE-round-3 code. Restoring penev_A's round-2 snapshot
  (`model.after_bibliography_v2.json`, built before `ensure_reference_stub`
  started stamping `added_by` on a stub) and running `bibliography --force`
  twice lands on a STABLE but wrong 26 Citations / 17 keys / 52 References /
  162 edges both times, not 81/52/52/81: those 52 stub References carry no
  `added_by` at all, so the new cleanup retracts their 80 orphaned Citations
  but not the stubs themselves, and the 52 survivors' anchors then exclude
  most of the document's citation-bearing lines from re-detection
  (`ref_anchors`). Any document that had `pdfdrill bibliography` run on it
  BEFORE this fix lands will hit this once, on its first post-upgrade
  `--force`; a document built fresh under the current code never will (a
  `model --force`-only baseline runs clean at 81/52/52/81 twice — Scenario B
  in `run4_script.py`/`run4_result.json`, alongside the literal-ask Scenario
  A above). Fixing it would mean a migration pass or a different retraction
  strategy for objects with no `added_by` at all, which is a design
  decision, not this round's one item.
- **Also still open (same shape of bug, different call path, NOT fixed —
  the ruling scoped this round to `--force` specifically):** the
  `if existing and not force: return` early-return gate right above the
  fixed cleanup uses the SAME "does a non-stub Reference exist" test to
  decide "did bibliography already run, use the cache" on a bare (non
  `--force`) call. A document whose first `cmd_bibliography` run created
  only stubs (true whenever the heuristic text parse finds 0 entries, as on
  penev_A) will read as "never ran" on a second bare call too, and since
  that branch never reaches the (now-fixed) cleanup at all, it would
  redetect and duplicate WITHOUT `--force`, the same way `--force` did
  before this round.
- **New, separate defect found while updating
  `test_detect_author_year_in_objects_square_brackets_and_folding` (NOT
  fixed):** `ensure_reference_stub`'s exact-citekey-only lookup can pre-empt
  `link_citations`'s fuzzy surname+year PREFIX match. An abbreviated citekey
  (`"carlsson2009"`, folded from `(Carlsson, 2009)`) that would have
  fuzzy-matched a fuller gold Reference key (`"carlsson2009topology"`) via
  `link_citations`'s `ck.startswith(c)` logic instead gets `ensure_reference_
  stub`'s own brand-new spare stub, because that runs FIRST (at citation-
  creation time, doing only exact matching) and `link_citations`'s smarter
  match never gets a turn — the citation is already "linked" (to the wrong
  target) by the time `link_citations` looks at it. Reference count is
  therefore sometimes ONE MORE than distinct real-world works cited when
  citekeys are abbreviated inconsistently with the gold bibliography's
  fuller keys. Not encountered in the penev_A corpus measurement (its
  citekeys are already full `Surname+Year`, generated by the SAME folding
  `detect_author_year_citations`/`detect_author_year_in_objects` use), only
  in the unit-test fixture; flagged rather than guessed at a fix, since
  resolving it changes which of the two matching strategies (exact-first vs.
  fuzzy-first) wins and that's a design call, not a bug fix.
- The retraction gap itself (HANDOVER-RULES §3) is unfixed, as instructed.
- `docs/layers/PROPS.md` / `corpus_props.json` do not yet list `stub` (new
  prop) or `ref_source: "citation"` (new value of an existing prop) — both
  require a `tools/propstable.py` run after a fresh corpus walk this task
  did not perform. No test currently requires it.
- `cmd_bibfetch`'s stub-clearing (API and keyless-delegate paths) has no
  dedicated new test; `tests/test_bibfetch_delegate.py` covers its baseline
  behaviour and passed unchanged.

---

## Round 4 — the model-side findings of the 010 review

All nine items applied; none parked. One item (6) could not use the ruling's
named helper and used the smallest correct alternative instead — stated below
rather than silently. Measurement: penev_A **81 / 52 / 52 / 81** twice in a
row, and the LaTeX lane **29 References == 29 distinct `\cite` keys**.

### The items, each with its test

| # | finding | fix | test |
|---|---|---|---|
| 1 | `link_citations_by_label` ignored the stub — two edges, two References, stub permanent | `citation.absorb_stub(doc, stub, into)`, called by every linker | `test_label_linker_absorbs_the_stub_into_the_gold_reference` |
| 2 | linkers returned "edges added"; `cmd_bibsource` read it as "citations linked" and wiped | linkers return `{"linked", "added"}`; all three callers read `linked` | `test_cmd_bibsource_does_not_wipe_correctly_linked_citations` |
| 3 | three "no References yet" gates never fired | `bibliography.has_filled_references` + `filled_cites_edges` | `test_has_filled_references_ignores_stubs`, `test_auto_bibliography_gate_fires_on_a_stub_only_document`, `test_tiddlers_gate_fires_on_a_stub_only_document`, `test_citation_pass_builds_when_only_stubs_exist`, `test_citation_pass_already_counts_only_filled_references` |
| 4 | exact-citekey stub pre-empted the fuzzy surname match | a stub-linked Citation counts as UNRESOLVED in `find_ref`; a fuzzy hit absorbs the stub | `test_fuzzy_surname_match_beats_the_citations_own_stub`, and `test_bibsource.py::test_detect_author_year_in_objects_square_brackets_and_folding` (updated to the fixed shape) |
| 5 | `added_by` retraction deleted a gold-FILLED Reference | `_retract` keeps a Reference that is filled and not `ref_source: "text"` | `test_bibliography_force_keeps_a_gold_filled_reference` |
| 6 | `cmd_annotate --force` blanket-cleared `cites` | clears only edges on the `links` stream (its own) | `test_annotate_force_keeps_the_citation_to_reference_edges` |
| 7 | two Citation creators minted no stub | `latex_source` and `markdown_source` call `ensure_reference_stub` / `add_cites_alignment` | `test_latex_source_cite_mints_a_stub`, `test_markdown_cite_without_a_reference_mints_a_stub` |
| 8 | `stub` absent from PROPS.md / props_code.json | `tools/propscan.py` + `tools/propstable.py` regenerated; `stub` merged into the corpus snapshot from a fresh walk | `tests/test_type_contract.py` (prop contract, 29 passed) |
| 9 | `add_cites_alignment`'s "one place" claim was aspirational | made true, and the docstring now names every caller and the one look-alike | (covered by 1/4/7) |

All twelve new tests were written first and confirmed failing for their stated
reason (`12 failed in 0.24s`) before any implementation.

### Item 6 — the ruling could not be applied literally

The ruling was "use `drop_dangling_cites` (or leave `cites` alone)".
`drop_dangling_cites` keeps an edge whose RIGHT range resolves to some
Reference — and a stub Reference is anchored at its citation's OWN line, so
`annotate`'s Link→Citation edges (right = a Citation's line range) resolve
against the stub sitting at that same range and would SURVIVE the clear, then
be re-added by `link_xref_alignments` as duplicates. The smallest correct
alternative: `cmd_annotate --force` retracts only the edges it made, which are
exactly the ones anchored on the `links` stream (`a.left.stream == "links"`).
Model-build Citation→Reference edges are never on that stream, so they are
untouched — which is what "`cites` is a model-build invariant" asks for.

### Item 8 — what could and could not be regenerated

`tools/propscan.py` now finds `stub` with a writer
(`docmodel/modules/citation.py`) and four readers. `docs/layers/PROPS.md` is
generated from the CORPUS half, so `stub` could only appear there once the
corpus snapshot carried it. A full read-only re-walk of `~/pdfdrill-library`
(1365 models, 3m27s; `scratchpad/propwalk.py`) measured `stub` at **52
Reference objects in 1 document**, and that one entry was merged into
`corpus_props.json` with its provenance recorded in the file. The same walk
found **17 other props** the 290 snapshot predates (`derived`, `derived_from`,
`file`, `font_size`, `labels`, `level_basis`, `line_count`,
`mathpix_subtype`, `options`, `parent_id`, `parent_type`, `quad`,
`refined_author`, `refined_basis`, `refined_note`, `resolved`, `rows`) —
**none of which has a reader**, so admitting them would demand 17 researched
`NO_READER_REASON` entries. That is a corpus-refresh task, not this one; it is
recorded in the snapshot's `_provenance` so the next reader finds it. Both
prop-contract checks pass.

### THE MEASUREMENT — round 5 of the table

`~/pdfdrill-library/penev_A/out/010/round4_script.py` → `round4_result.json`,
both halves through the real CLI entry points; re-running reproduces them.
Backup taken first: `out/010/model.before_round4.json` (+ `.docpack.json`).

| state | Citations | keys | References | stubs | `cites` |
|---|---:|---:|---:|---:|---:|
| round 5: after `model --force` alone | 1 | 1 | 1 | 1 | 1 |
| round 5: after + `bibliography --force` (run 1) | **81** | **52** | **52** | **52** | **81** |
| round 5: after + `bibliography --force` (run 2) | **81** | **52** | **52** | **52** | **81** |

The LaTeX lane (item 7), on a COPY of arXiv 2010.14265's e-print
`texsrc/paper.tex` through `pdfdrill latexbook --no-svg` (the command that
runs `latex_source.build_source_model`): **68 Citations** (one per `\cite`
occurrence, matching the 68 occurrences in the source), **29 distinct
citekeys**, **29 References** — equal to the 29 distinct `\cite` keys the
source names — 29 stubs, 68 `cites` edges. Before this round that lane
produced 68 Citations and **zero** References. The library document was not
written: the measurement copies `texsrc/` to a temp dir.

### TESTS

    PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest tests/ -q -p no:cacheprovider
    -> 3119 passed, 0 failed

Updated, not weakened: `test_bibliography.py` (3 assertions on the linkers'
new `{linked, added}` return), `test_bibsource.py::test_ingest_bbl_and_enrich_
and_link` (same), `test_reference_stub.py::test_link_citations_is_idempotent_
against_ensure_reference_stub` (same), and
`test_bibsource.py::test_detect_author_year_in_objects_square_brackets_and_
folding`, which had PINNED the item-4 defect as the known shape — it now
asserts the fixed shape (the abbreviated `carlsson2009` reaches the fuller gold
`carlsson2009topology` and its spare stub is absorbed, leaving 3 References,
none of them a stub).

### OPEN after this round

- **The 17 unread props above.** Real corpus drift since the 290 snapshot,
  found by this round's walk, out of scope to admit here.
- **`cmd_bibliography`'s `if existing and not force: return` early-return
  gate** (flagged in round 3, still unfixed) now reads `existing` = non-stub
  References, which after this round is the same `has_filled_references`
  question. It is a cache gate, not a correctness gate, and the ruling scoped
  round 3 to `--force`; left as recorded.
- **The pre-migration upgrade caveat** (round 3's OPEN, addressed by 639's
  `drop_ownerless_stubs`) is unchanged.
- **The "8 filled by bibsource" half of the spec** still cannot be measured on
  penev_A (no `.bbl`/`.bib` on disk); it stays pinned by unit test.
- The retraction gap itself (HANDOVER-RULES §3) is unfixed, as instructed.
