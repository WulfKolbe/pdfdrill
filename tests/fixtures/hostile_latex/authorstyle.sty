%% An author's own style file, sitting beside the document rather than in
%% texmf. collect_macros resolves \usepackage{authorstyle} to THIS file
%% because it is local; amsmath and friends are skipped as system packages.
\ProvidesPackage{authorstyle}
\newcommand{\styleMacro}[1]{\mathbf{#1}}
